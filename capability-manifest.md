# Capability manifest — UniExtract2 → rusty_extract

Source: https://github.com/mzelivsky-spec/UniExtract2 (AutoIt, commit at time of
inventory: see `docs/adr/` bootstrap PR). Target: this repo, staged migration,
phase 1 = core file-type detection + extraction-orchestration engine as a Rust
CLI/library. Windows-only parity target. Shells out to the same external helper
binaries as the source (user decision, 2026-08-17 — see `ARCHITECTURE.md`).

This is the boundary contract in concrete form (see the `rust-migration` skill's
`SKILL.md`): every row defaults to **REQUIRED** on creation. A row only becomes
**OUT-OF-SCOPE** with a written, user-attributed reason. `DONE` requires a merged
PR and a named parity test in **Evidence** — "compiles" is not evidence.

**RustyMill sibling check**: cross-checked capability names/purposes against
`references/platform-directory.md`'s one-line descriptions for every
`Rusty-Mill/*` and `baileyrd/rusty_*` repo. None describe archive/installer
extraction, file-type signature detection, or external-process-orchestration-
for-unpacking — the closest adjacent repos (`rustils` = OS abstraction,
`rusty_win32` = Win32 API bindings) could become *infrastructure* dependencies
later (process launch, registry access) but are not capability matches for any
row below. **Caveat**: this session could not attach `Rusty-Mill`-owned repos
for a grep-level check (cross-owner restriction — this session's other sources
are `baileyrd`-owned) — the check above is directory-description-level
judgment, not a verified `scan_platform_repos.sh` grep. Re-run a real scan in
a session that can attach `Rusty-Mill/*` before assuming "none found" is final,
per the skill's own stated limitation on this step. Every row below is marked
`none found (directory check only)` accordingly.

## Row groups

This file is **one continuous table** (`scripts/check_manifest_coverage.sh`
parses a single header/separator followed by every data row — see the
skill's `references/capability-manifest-format.md`). Row groups, by ID
prefix, in table order:

- **D001–D012 — Deferred subsystems** (staged migration, user decision
  2026-08-17; **D001–D005 and D007–D012 superseded 2026-08-23** — see
  below). This phase covers detection + extraction orchestration only.
  The GUI, Windows context-menu/registry integration, auto-updater,
  feedback/telemetry, uninstall routine, and full translation catalogs are
  deferred to a later phase — **tracked, not dropped**. Per the skill's
  step-0 allowance for partial migrations, these subsystems are not
  inventoried capability-by-capability in this pass (that's the later
  phase's own step-1 job); they're recorded here as coarse rows so the
  manifest stays honest about what this run's coverage check does and
  doesn't cover.
- **C183–C217 — GUI/tray/updater/telemetry/uninstall phase** (phase 2,
  user decision 2026-08-23: bring D001 (main GUI/tray shell), D002
  (context-menu/file-association), D003 (auto-updater), and D004
  (feedback/telemetry) into scope now, as a strict 1:1 port using `egui`
  for rendering — same parity discipline as phase 1). D005/D007–D012 are
  pure sub-parts of those four (CLI verbs dispatching into them, or
  preferences configuring them) and come into scope with their parent;
  D006 (full non-English translation catalogs) stays deferred — see its
  own row. This phase's own inventory (step 1) was done via parallel
  read-only research across the ~2,900-line GUI/updater/feedback/uninstall
  section of `UniExtract.au3` (lines 3735-8197 plus scattered CLI-dispatch
  and preference-key sites elsewhere in the file); grouped by cohesive
  subsystem rather than one row per AutoIt function, same granularity as
  phase 1's own rows. **Cross-phase reuse already identified**: C053/C055
  already cover `GUI_MethodSelect`/`GUI_MethodSelectList`'s decision logic
  (only the dialog rendering is new here, not tracked as a separate row —
  it's covered by whichever new row's GUI layer ends up hosting that
  prompt); `_WinGetByPID` is the exact same mechanism as
  `automation::win32`'s existing `enum_window_by_pid_proc` (C069/C166) —
  reuse it, don't re-port; C166 already ported the tray status popup's
  *text-update* call only, not the popup window's own lifecycle (create/
  position/fade-in/fade-out/round-corners/hide-toggle), which is new (see
  C185). **Known infrastructure gap, not yet closed**: `automation::win32`
  currently only supports DWORD registry values under `HKCU`/`HKLM` —
  C202's context-menu registry writes need `HKEY_CLASSES_ROOT` plus
  `REG_SZ`/`REG_EXPAND_SZ` value writes, and the deletion semantics need
  re-verifying against AutoIt's own recursive `RegDelete` behavior; that
  extension is scoped as part of C202's own implementation, not a
  separate row. **RustyMill sibling check**: same caveat as above — this
  session couldn't attach `Rusty-Mill`-owned repos for a real grep-level
  check; `rusty_win32` (Win32 API bindings) is the one repo worth
  re-checking for existing HKCR/string-registry-write support before
  hand-extending `automation::win32`, and `rusty_http`/`rusty_request`
  for the feedback system's multipart POST and the updater's HTTP GET, once a
  session that can attach `Rusty-Mill/*` runs this check for real. No
  sibling repo in the directory provides a GUI toolkit or tray-icon
  primitive, consistent with the user's own choice of `egui` (a new
  external dependency) for this phase.
- **C001–C016 — CLI interface** (phase 1: core engine)
- **C017–C036 — Configuration** (phase 1: core-engine-relevant preferences)
- **C037–C055 — Detection engine**
- **C056–C137 — Extractor integrations**, one row per distinct external
  helper binary/tool integration UniExtract2 shells out to. Each ports the
  same external binary per the migration's own scope decision
  (2026-08-17) — the Rust work is the orchestration/invocation code, not a
  reimplementation of the tool itself.
- **C138–C182 — Runtime extraction behavior**

| ID | Capability | Category | Source | Existing RustyMill impl | Status | Reason (if OUT-OF-SCOPE) | Evidence |
| --- | --- | --- | --- | --- | --- | --- | --- |
| D001 | Main GUI: window, dialogs, tray icon, status popup, preferences dialog, plugin manager, batch queue UI, first-start wizard | interface | code | none found (directory check only) | OUT-OF-SCOPE | Superseded 2026-08-23 (user decision): promoted into scope and inventoried in detail — see C183-C201, C205 (GUI plugin-manager/dispatch pieces), C216-C217. This row is retired, not re-deferred; it fulfilled its own stated purpose ("to be inventoried in detail when that phase starts"). | |
| D002 | Windows context-menu / shell registration (`HKCU\Software\UniExtract`) and file-association management | behavior | code | none found (directory check only) | OUT-OF-SCOPE | Superseded 2026-08-23 (user decision): promoted into scope — see C201-C204. | |
| D003 | Auto-updater: `CheckUpdate()`, nightly/beta channel switch, update-interval scheduling | behavior | code | none found (directory check only) | OUT-OF-SCOPE | Superseded 2026-08-23 (user decision): promoted into scope — see C205-C211. | |
| D004 | Feedback/telemetry system: feedback prompt + submission, usage-statistics send, per-install GUID | behavior | code | none found (directory check only) | OUT-OF-SCOPE | Superseded 2026-08-23 (user decision): promoted into scope — see C212-C215. | |
| D005 | Uninstall routine (`Uninstall()`) | behavior | code | none found (directory check only) | OUT-OF-SCOPE | Superseded 2026-08-23 (user decision): promoted into scope — see C216-C217. | |
| D006 | Full translation catalogs (`lang/*.ini` localized string packs beyond a default English set) | docs | docs+code | none found (directory check only) | OUT-OF-SCOPE | User decision, 2026-08-17, reaffirmed 2026-08-23: deferred. Unlike D001-D005/D007-D012, this one is **not** superseded — it's genuinely separate content-authoring work (translating strings into other languages), not implied by porting the GUI shell itself. C183-C217's own dialogs still need a default English string set (that's ordinary GUI-text work, tracked within each row, not a separate capability) but full non-English `lang/*.ini` packs stay out of scope until a user decision says otherwise. | |
| D007 | `/afterupdate`, `/update`, `/updatehelper`, `/updatehelpers` CLI verbs | interface | code | none found (directory check only) | OUT-OF-SCOPE | Superseded 2026-08-23 (user decision): promoted into scope — see C205. | |
| D008 | `/plugins` CLI verb | interface | code | none found (directory check only) | OUT-OF-SCOPE | Superseded 2026-08-23 (user decision): promoted into scope — see C192. | |
| D009 | `/uninstall`, `/removeuserdata` CLI verbs | interface | code | none found (directory check only) | OUT-OF-SCOPE | Superseded 2026-08-23 (user decision): promoted into scope — see C216. | |
| D010 | GUI-only preferences: `notraycon`, `nostatusbox`, `hidestatusboxiffullscreen`, `openfolderafterextr`, `keepopen`, `storeguiposition`+`posx`/`posy`/`GuiWidth`/`GuiHeight`, `topmost`, `statusposx`/`statusposy`, GUI-text-field `%VAR%` env-placeholder expansion (`EnvParse`) | config | code | none found (directory check only) | OUT-OF-SCOPE | Superseded 2026-08-23 (user decision): promoted into scope, folded into whichever of C183-C190/C199 owns each preference (not tracked as its own row). | |
| D011 | Context-menu/file-association preferences: `addassocenabled`, `addassoc`, `addassocallusers` | config | code | none found (directory check only) | OUT-OF-SCOPE | Superseded 2026-08-23 (user decision): promoted into scope — see C204. | |
| D012 | Telemetry/updater preferences: `feedbackprompt`, `sendstats`, `updateinterval`, `lastupdate`, `ID` (per-install GUID), `Statistics` ini section | config | code | none found (directory check only) | OUT-OF-SCOPE | Superseded 2026-08-23 (user decision): promoted into scope — folded into C207 (updateinterval/lastupdate), C213 (feedbackprompt), C214 (sendstats), C215 (ID), C196 (Statistics section). | |

| C001 | Positional file argument: path to extract/scan, resolved to a full path; nonexistent path terminates with exit code 5 | interface | code | none found (directory check only) | DONE | | PR [#364](https://github.com/baileyrd/rusty_extract/pull/364), tests `file_arg::tests::resolve_file_argument_path_matches_source_branches`, `file_arg::tests::validate_file_argument_matches_source_branches` |
| C002 | Positional destination argument: output directory, or the literal tokens `/sub`, `/last`, `/scan` | interface | code | none found (directory check only) | DONE | | PR [#365](https://github.com/baileyrd/rusty_extract/pull/365), test `dest_arg::tests::other_values_are_path_full_resolved_against_cwd` (plus `sub_and_last_tokens_pass_through_unresolved`, `absent_when_no_second_argument`) |
| C003 | `/scan` — scan-only mode: detect and report file type, do not extract | interface | code+docs | none found (directory check only) | DONE | | PR [#365](https://github.com/baileyrd/rusty_extract/pull/365), test `dest_arg::tests::scan_token_enters_scan_only_mode_case_insensitively`. Covers only the routing decision and its `extract`/`create_log` flags; the "detect and report file type" half is C037-046 (detection engine, not yet wired into this phase's `main.rs`) plus C153/C154 (scan-only output, already DONE) |
| C004 | `/sub` destination token — extract into a subdirectory named after the archive | interface | code | none found (directory check only) | DONE | | PR [#257](https://github.com/baileyrd/rusty_extract/pull/257), test `outdir::tests::sub_token_resolves_to_initoutdir` |
| C005 | `/last` destination token — extract into the most-recently-used output directory (history) | interface | code | none found (directory check only) | DONE | | PR [#257](https://github.com/baileyrd/rusty_extract/pull/257), tests `outdir::tests::get_last_outdir_matches_source`, `outdir::tests::last_token_resolves_to_last_outdir` |
| C006 | `/type[=value]` override — force a specific extractor, bypassing all detection; with no value, present a candidate list for the caller to choose | interface | code | none found (directory check only) | DONE | | PR [#366](https://github.com/baileyrd/rusty_extract/pull/366), tests `type_override::tests::*` (6 tests). Covers the routing/parsing decision only; building the candidate type-name list (real filesystem I/O) is caller-supplied, and the GUI candidate-list prompt itself is deferred (manifest row D001) |
| C007 | `/silent` — suppress all interactive prompts for this run | interface | code+docs | none found (directory check only) | DONE | | PR [#255](https://github.com/baileyrd/rusty_extract/pull/255), test `cli::tests::silent_flag_detected_case_insensitively` |
| C008 | `/nolog` — suppress the per-run log file for this invocation, overriding the persisted `log` preference | interface | code | none found (directory check only) | DONE | | PR [#255](https://github.com/baileyrd/rusty_extract/pull/255), test `cli::tests::nolog_flag_detected_case_insensitively` |
| C009 | `/nostats` flag parsing — accepted without error; actual stats-send behavior is D004 | interface | code | none found (directory check only) | DONE | | PR [#255](https://github.com/baileyrd/rusty_extract/pull/255), test `cli::tests::nostats_flag_detected_case_insensitively` |
| C010 | `/help`, `/?`, `-h`, `/h`, `-?`, `--help` — print CLI usage/help text, exit 0 | interface | code | none found (directory check only) | DONE | | PR [#255](https://github.com/baileyrd/rusty_extract/pull/255), test `cli::tests::help_flag_matches_all_six_spellings_case_insensitively` |
| C011 | `/batch` — queue the file for later processing instead of extracting immediately | interface | code+docs | none found (directory check only) | DONE | | PR [#367](https://github.com/baileyrd/rusty_extract/pull/367), test `cli::tests::batch_flag_detected_case_insensitively`. Flag detection only; queuing the entry is `batch::build_command_line` (C147, already DONE) and the caller's job |
| C012 | `/batchclear` — clear the batch queue | interface | code | none found (directory check only) | DONE | | PR [#255](https://github.com/baileyrd/rusty_extract/pull/255), test `cli::tests::batchclear_flag_matches_case_insensitively` |
| C013 | `/close` — exit silently (used to signal a running instance to close) | interface | code | none found (directory check only) | DONE | | PR [#255](https://github.com/baileyrd/rusty_extract/pull/255), test `cli::tests::close_flag_detected_case_insensitively` |
| C014 | Directory passed as the file argument — queues every file within as a batch instead of erroring or treating the directory itself as input | behavior | code | none found (directory check only) | DONE | | PR [#368](https://github.com/baileyrd/rusty_extract/pull/368), test `entry_gate::tests::directory_input_queues_as_batch` (plus `directory_input_gate_does_not_fire_for_a_file`). Routing/status decision only; directory enumeration is `GUI_Batch_AddDirectory` (deferred GUI subsystem, D001) |
| C015 | Single-running-instance behavior — a second invocation while one is running is queued via the batch mechanism instead of running concurrently | behavior | code | none found (directory check only) | DONE | | PR [#368](https://github.com/baileyrd/rusty_extract/pull/368), test `entry_gate::tests::second_instance_queues_when_mutex_held_and_would_extract` (plus 2 more). Routing/status decision only; OS mutex acquisition is caller-supplied |
| C016 | Exit code contract — internal status maps to a specific numeric process exit code (0 success, 1 failed, 3 unknown exe, 4 unknown ext/scan-fail, 5 invalid file/dir, 6 not packed, 7 not supported, 8 missing exe/plugin, 9 timeout, 10 wrong password, 11 missing definition, 12 move failed, 13 no free space, 14 missing archive part; syntax/batch/silent paths exit 0) | interface | code | none found (directory check only) | DONE | | PR [#237](https://github.com/baileyrd/rusty_extract/pull/237), tests `status::tests::fixed_exit_codes_match_source`, `status::tests::fileinfo_exit_code_depends_on_silent_mode_and_filetype_identification` |

| C017 | `language` preference — selects the message-string language file; auto-detected from OS locale on first run if unset/invalid | config | code | none found (directory check only) | DONE | | PR [#369](https://github.com/baileyrd/rusty_extract/pull/369), tests `prefs::tests::resolve_language_*` (4 tests). `has_translation`/OS-locale candidates are caller-supplied; full translation catalogs are out of scope (D006) |
| C018 | `batchqueue` preference — overrides the batch queue file path (default `%settingsdir%\batch.queue`) | config | code | none found (directory check only) | DONE | | PR [#249](https://github.com/baileyrd/rusty_extract/pull/249), test `prefs::tests::batchqueue_path_matches_source_default_and_override` |
| C019 | `filescanlogfile` preference — overrides the scan-only result log path (default `%settingsdir%\log\filescan.txt`) | config | code | none found (directory check only) | DONE | | PR [#249](https://github.com/baileyrd/rusty_extract/pull/249), test `prefs::tests::filescanlogfile_path_matches_source_default_and_override` |
| C020 | `batchenabled` — persisted flag driving batch-queue continuation logic on process exit | config | code | none found (directory check only) | DONE | | PR [#247](https://github.com/baileyrd/rusty_extract/pull/247), test `prefs::tests::batchenabled_preference_default_matches_source` |
| C021 | `history` preference — records the last 10 used files/output directories | config | code | none found (directory check only) | DONE | | PR [#253](https://github.com/baileyrd/rusty_extract/pull/253), tests `prefs::tests::push_history_prepends_new_item`, `prefs::tests::push_history_deduplicates_and_moves_to_front`, `prefs::tests::push_history_ten_entry_cap_does_not_backfill_a_deduped_slot`, `prefs::tests::push_history_from_empty_history` |
| C022 | `appendext` preference — controls whether an extension is appended to extracted output | config | code | none found (directory check only) | DONE | | PR [#247](https://github.com/baileyrd/rusty_extract/pull/247), test `prefs::tests::appendext_preference_default_matches_source` |
| C023 | `warnexecute` preference — warn before running/executing self-extracting content | config | code | none found (directory check only) | DONE | | PR [#247](https://github.com/baileyrd/rusty_extract/pull/247), test `prefs::tests::warnexecute_preference_default_matches_source` |
| C024 | `deletesourcefile` preference — keep/delete/ask/move source archive after a successful extraction | config | code | none found (directory check only) | DONE | | PR [#241](https://github.com/baileyrd/rusty_extract/pull/241), test `prefs::tests::delete_source_file_option_parses_autoit_enum_numbering` |
| C025 | `freespacecheck` preference — enable/disable a disk-space check before extraction | config | code | none found (directory check only) | DONE | | PR [#247](https://github.com/baileyrd/rusty_extract/pull/247), test `prefs::tests::freespacecheck_preference_default_matches_source` |
| C026 | `Timeout` preference — extraction timeout in seconds; minimum enforced 10s, default 60s if invalid | config | code | none found (directory check only) | DONE | | PR [#239](https://github.com/baileyrd/rusty_extract/pull/239), tests `prefs::tests::stored_seconds_convert_to_milliseconds`, `prefs::tests::values_under_ten_seconds_reset_to_the_sixty_second_default`, `prefs::tests::exactly_ten_seconds_is_not_reset`, `prefs::tests::missing_preference_key_reproduces_the_sixty_million_millisecond_quirk` |
| C027 | `keepoutputdir` preference | config | code | none found (directory check only) | DONE | | PR [#247](https://github.com/baileyrd/rusty_extract/pull/247), test `prefs::tests::keepoutputdir_preference_default_matches_source` |
| C028 | `log` preference — enable/disable a per-extraction debug log file; overridable per-run by `/nolog` (C008) | config | code | none found (directory check only) | DONE | | PR [#247](https://github.com/baileyrd/rusty_extract/pull/247), test `prefs::tests::log_preference_default_matches_source` |
| C029 | `extract` preference — persisted default for extract-vs-scan-only; overridden per-run by `/scan` (C003) | config | code | none found (directory check only) | DONE | | PR [#247](https://github.com/baileyrd/rusty_extract/pull/247), test `prefs::tests::extract_preference_default_matches_source` |
| C030 | `unicodecheck` preference — enables detection/handling of non-ASCII filenames requiring temp rename | config | code | none found (directory check only) | DONE | | PR [#247](https://github.com/baileyrd/rusty_extract/pull/247), test `prefs::tests::unicodecheck_preference_default_matches_source` |
| C031 | `extractvideotrack` preference — controls whether video-track extraction is attempted for media files | config | code | none found (directory check only) | DONE | | PR [#247](https://github.com/baileyrd/rusty_extract/pull/247), test `prefs::tests::extractvideotrack_preference_default_matches_source` |
| C032 | `silentmode` preference — persisted default for silent mode, independent of the per-run `/silent` flag (C007) | config | code+docs | none found (directory check only) | DONE | | PR [#247](https://github.com/baileyrd/rusty_extract/pull/247), test `prefs::tests::silentmode_preference_default_matches_source` |
| C033 | `cleanup` preference — post-extraction cleanup mode (move-to-subfolder vs delete) | config | code | none found (directory check only) | DONE | | PR [#243](https://github.com/baileyrd/rusty_extract/pull/243), test `prefs::tests::cleanup_option_parses_autoit_enum_numbering_with_move_fallback` |
| C034 | `BatchRecurse` preference — recurse into subdirectories when batch-adding a directory (C014) | config | code | none found (directory check only) | DONE | | PR [#251](https://github.com/baileyrd/rusty_extract/pull/251), test `prefs::tests::batchrecurse_preference_default_matches_source` |
| C035 | Password list file — plain text, one password per line, default `%settingsdir%\passwords.txt`, falls back to `@ScriptDir\passwords.txt` | config | code | none found (directory check only) | DONE | | PR [#245](https://github.com/baileyrd/rusty_extract/pull/245), test `prefs::tests::password_list_path_matches_source_fallback` |
| C036 | Third-party detector tool silencing — force PEiD/Exeinfo-adjacent bundled tools into non-interactive mode via their own registry keys, with backup/restore of prior values, so automated detection doesn't hang on a tool's own GUI | behavior | code | none found (directory check only) | DONE | | PR [#375](https://github.com/baileyrd/rusty_extract/pull/375), tests `detector_silence::tests::silence_value_sets_match_source`, `detector_silence::tests::restore_plan_restores_when_key_existed`, `detector_silence::tests::restore_plan_deletes_when_key_did_not_exist`, `detector_silence::tests::restore_plan_deletes_for_empty_backup`; UniExtract.au3:1286-1324,1822-1850 |

| C037 | Layered detection cascade order: extension pre-check → PE-aware (Exeinfo/PEiD) for `.exe`/`.dll` → TrID → Unix `file` as second opinion → extension fallback → blind 7z probe. Order itself is behavior-significant. | behavior | code | none found (directory check only) | DONE | | `detection::cascade::steps` ports `StartExtraction()`'s step order after `InitialCheckExt()` (C046), gated on file extension and extract-vs-scan-only mode; each named step is its own separately-tracked capability (C038-C045, C077, C047, C048) — this covers the order/gating between them. Non-obvious finding: in extract mode, an exe/dll file is delegated entirely to `IsExe()`, which never returns control (it always terminates internally) — none of `StartExtraction()`'s other steps run for such a file; only a non-exe file in extract mode reaches the full remaining cascade. PR [#391](https://github.com/baileyrd/rusty_extract/pull/391), tests in `detection::cascade::tests` (4). UniExtract.au3:376-463, comment 424-430 |
| C038 | TrID signature-based detection (TrIDLib.dll via DllCall) | behavior | code | none found (directory check only) | DONE | | **Decision logic/invocation-building, PR [#399](https://github.com/baileyrd/rusty_extract/pull/399); the dispatch table itself is C039, `DONE`.** `detection::trid_scan` covers: `scan_invocation` (scan-only-mode `trid.exe` command line, including the conditional `-v` flag); `extract_result_action` (which of TrID's candidate results rename-on-extension/dispatch to `tridcompare`, capped at the first 3); `should_keep_scan_only_line` (the verbose-output line filter). **The `TrIDLib.dll` calls are ported too, PR [#416](https://github.com/baileyrd/rusty_extract/pull/416):** new `dlllib` module — `TridLibrary` trait (`load_defs_pack`/`submit_file`/`analyze`/`result_count`/`result_type`/`result_extension`, matching `TrID_LoadDefsPack`/`TrID_SubmitFileA`/`TrID_Analyze`/`TrID_GetInfo`), `tridlib_load`/`tridlib_analyse`/`tridlib_analyse_simple` orchestration, a `FakeTridLibrary` test double, and a real `Win32TridLibrary` (`LoadLibraryW`/`GetProcAddress` + hand-written function-pointer types per export — not a generic dynamic-call shim, since only 4 exports are ever needed). Found and preserved a genuine quirk: `TridLib_Load`'s `$hTridDll` cache is set *before* the definitions-pack result is checked, so a failed load still looks "already loaded" to the reentry guard. Carries the same honesty caveat as C069: fake-backed tests prove the decision logic, not that the real DLL is called correctly (neither the licensed DLL nor its definitions pack exists in this environment or on CI). `FetchStdout` (scan-only mode's real process execution) stays out of scope. Tests: `detection::trid_scan::tests` (8), `dlllib::tests`/`dlllib::fake::tests` (12). UniExtract.au3:901-1030,944-1001 |
| C039 | TrID match dispatch table — maps TrID's textual output substrings to extractor types (~130 cases) | behavior | code | none found (directory check only) | DONE | | `detection::trid_dispatch::classify` ports `tridcompare`'s full `Select` (92 `Case` clauses), matched top to bottom exactly as the source orders it; all 134 literal needle strings verified present in the exact cited source range. Preserves the table's one case-sensitive comparison (`'(.EXE)', 1`, unique among ~90 otherwise case-insensitive cases) and two genuine dead-code quirks (`"null bytes"` shadowed by the earlier disk-image group; the generic `Executable`/`IsExe` case correctly never fires for ELF binaries since "Not packed" is checked first). `Case Else` already covered by `detection::detector_mapping::resolve_trid` (C051), not duplicated. PR [#394](https://github.com/baileyrd/rusty_extract/pull/394), tests in `detection::trid_dispatch::tests` (16). UniExtract.au3:1490-1801 (tridcompare) |
| C040 | Unix `file` tool as secondary/second-opinion detector, run automatically after TrID | behavior | code | none found (directory check only) | DONE | | `detection::unixfile_scan::clean_output` + `scan_outcome` port `FileScan_UnixFile`'s output cleanup and post-scan branch; the unconditional call right after TrID (line 938) is the "run automatically" half of this capability, not a decision this module makes. Extract mode hands off entirely to `detection::file_dispatch::classify` (C041), not duplicated. Real process I/O (`FetchStdout`) stays out of scope, same boundary as elsewhere in this port. PR [#395](https://github.com/baileyrd/rusty_extract/pull/395), tests in `detection::unixfile_scan::tests` (6). UniExtract.au3:1033-1051,938 |
| C041 | Unix `file` match dispatch table (~25 cases), including flagging un-extractable text/media types as "not packed" | behavior | code | none found (directory check only) | DONE | | `detection::file_dispatch::classify` + `trailing_termination` port `filecompare`'s full `Select` plus its two trailing not-packed/not-supported checks, matched top to bottom exactly as the source orders it; all 66 literal needle strings verified present in the exact cited source range. Genuine preserved quirk: `"POSIX tar archive"` is unreachable in practice — `"tar archive"` always contains the earlier `"ar archive"` case as a substring. `Case Else` already covered by `detection::detector_mapping::resolve_file` (C051), not duplicated. PR [#393](https://github.com/baileyrd/rusty_extract/pull/393), tests in `detection::file_dispatch::tests` (15). UniExtract.au3:1398-1487 (filecompare) |
| C042 | Exeinfo PE detection, run before TrID for `.exe`/`.dll` files | behavior | code | none found (directory check only) | DONE | | **Scope — scan orchestration; the dispatch table itself is C043, `DONE`.** `detection::exeinfo_scan` covers everything around the `Select`: `scan_invocation` builds the extract-mode command-line probe (`RunWait` + log file, a real command-line call, **not** GUI automation — a non-obvious finding, since `$bUseCmd` defaults to `$extract`, PR [#397](https://github.com/baileyrd/rusty_extract/pull/397)); `should_retry_as_gui`, `strip_filename_prefix`, and `classify_scan_result` port the corrupted/too-big/not-exe-or-dll/scan-only-mode branches. **The scan-only-mode GUI path is now ported too**, PR [#412](https://github.com/baileyrd/rusty_extract/pull/412): `scan_via_gui` (built on C069's new `automation::GuiAutomation` infrastructure) reproduces `OpenExeInfo()` + the `TEdit6`-polling loop (always polls at least once, since `$sFileType`'s seed value trivially satisfies the loop condition) + appending `TEdit5`'s text + `CloseExeInfo()`. Carries the same honesty caveat as C069: fake-backed tests prove the decision logic, not that the real Win32 backend drives an actual Exeinfo PE window. Tests: `detection::exeinfo_scan::tests` (13). UniExtract.au3:1096-1281,466 |
| C043 | Exeinfo PE match dispatch table (~45 cases), including explicit "not supported"/"not packed" terminal cases | behavior | code | none found (directory check only) | DONE | | `detection::exeinfo_dispatch::classify` ports `FileScan_ExeInfo`'s full `Select`, matched top to bottom exactly as the source orders it (preserves the `InstallAware`-before-`InstallShield` and `upx`-checked-last ordering comments); every one of the 64 literal needle strings verified present in the exact cited source range. `Case Else` already covered by `detection::detector_mapping::resolve_exeinfo` (C051), not duplicated. PR [#392](https://github.com/baileyrd/rusty_extract/pull/392), tests in `detection::exeinfo_dispatch::tests` (13). UniExtract.au3:1141-1278 |
| C044 | PEiD fallback packer-signature detection for executables Exeinfo PE couldn't classify (run twice: extension mode then hard mode) | behavior | code | none found (directory check only) | DONE | | **Dispatch table, PR [#396](https://github.com/baileyrd/rusty_extract/pull/396).** `detection::peid_dispatch::classify` ports `FileScan_Peid`'s full `Select` (UniExtract.au3:1332-1394, 20 cases), including its one case-sensitive comparison (`"PEtite", 1`) and the fact that, unlike C039/C041/C043, this `Select` has no `Case Else` fallback. **The scan itself is now ported too, PR [#413](https://github.com/baileyrd/rusty_extract/pull/413):** `detection::peid_scan::peid_scan` (built on C069's `automation::GuiAutomation` infrastructure) reproduces `Run`/`WinWait("PEiD v")`/the `Edit2`-polling loop/`WinClose`, reusing `detector_silence::PEID_KEY`/`PEID_SILENCE_VALUES`/`restore_plan` (C036) for the registry backup/restore rather than re-deriving it. **A genuine, preserved hang-risk quirk**: unlike `OpenExeInfo`'s/`RipExeInfo`'s own `WinWait` calls, this one passes no timeout at all — AutoIt's documented default (`0`) waits indefinitely; modeled as `u64::MAX` rather than the caller's `timeout_ms`, not "fixed" into a bounded wait. Carries the same honesty caveat as C069: fake-backed tests prove the decision logic, not that the real Win32 backend drives an actual PEiD window. Tests: `detection::peid_dispatch::tests` (8), `detection::peid_scan::tests` (5). UniExtract.au3:1284-1398,478-479 |
| C045 | MediaInfo scan — informational only, used in scan-only mode display, never drives extraction dispatch | behavior | code | none found (directory check only) | DONE | | **Formatting, PR [#398](https://github.com/baileyrd/rusty_extract/pull/398).** `detection::mediainfo_scan::format_media_info` ports the formatting pass exactly, including a genuinely easy-to-miss finding: `StringSplit($x, @CRLF, 2)` omits `$STR_ENTIRESPLIT`, so `@CRLF` splits as a *character set* (every lone `\r`/`\n`), not one two-character delimiter — roughly doubling the element count the `< 10` threshold checks against. Reproduced via `str::split(['\r','\n'])`. Also preserves the case-sensitive `"Complete name"` exclusion and the silent truncation when a line has more than one `" : "` occurrence. **The `MediaInfo.dll` calls are ported too, PR [#416](https://github.com/baileyrd/rusty_extract/pull/416):** `dlllib::scan_media_info` (`MediaInfoLibrary` trait wrapping `MediaInfo_New`/`_Open`/`_Inform`/`_Delete`, a `FakeMediaInfoLibrary` test double, and a real `Win32MediaInfoLibrary`) feeds its raw output straight into `format_media_info` (`detection::mediainfo_scan::tests::composes_with_dlllib_scan_media_info` proves the pipeline end to end). Carries the same honesty caveat as C069: fake-backed tests prove the decision logic, not that the real DLL is called correctly (neither the licensed DLL nor a real media file exists in this environment or on CI). Tests: `detection::mediainfo_scan::tests` (9), `dlllib::tests`/`dlllib::fake::tests` (12, shared with C038). UniExtract.au3:1054-1093,1060-1067 |
| C046 | Extension-based pre-check (split files `.001`, compound tar variants, unreliable-signature disk images) run before any signature scan | behavior | code | none found (directory check only) | DONE | | `detection::initial_ext_check::route` ports `InitialCheckExt`'s `Switch $fileext`; every routing target already has a home in this port (`detection::sevenzip_probe` C048, `extract::qbms` C077, `extract::ctar`/`extract::sevenzip`/`extract::unity` C181/C056/C054). Preserves a real quirk: `{bin,cdi,mdf}` calls `CheckIso()` then the 7-Zip probe, while `{cue,gdi,iso,mds}` calls them in the reverse order — modeled as distinct `Routing` variants. PR [#390](https://github.com/baileyrd/rusty_extract/pull/390), tests in `detection::initial_ext_check::tests` (7). UniExtract.au3:2193-2215 (InitialCheckExt) |
| C047 | Extension-based fallback dispatch — last resort via `def/registry.ini`'s `[Extensions]` section when no detector matched | behavior | code | none found (directory check only) | DONE | | PR [#185](https://github.com/baileyrd/rusty_extract/pull/185), test `detection::registry::tests::resolves_every_extension_in_the_bundled_registry` |
| C048 | Blind 7-Zip probe fallback — attempt a 7z listing regardless of detected type, as a final catch-all | behavior | code | none found (directory check only) | DONE | | PR [#188](https://github.com/baileyrd/rusty_extract/pull/188), test `detection::sevenzip_probe::tests` (7 tests covering `route`'s branches and `probe_invocation`) |
| C049 | Central extractor dispatcher — single function, ~70-case switch keyed on extractor-type constant | behavior | code | none found (directory check only) | DONE | | PR [#192](https://github.com/baileyrd/rusty_extract/pull/192), test `extract::dispatch::tests` (dispatch mechanism; `HARDCODED_CASES` grows with each extractor-integration capability) |
| C050 | Case-Else → `def/*.ini` plugin-engine fallback dispatch — any extractor-type string not hardcoded is treated as a plugin-ini filename stem | behavior | code | none found (directory check only) | DONE | | PR [#194](https://github.com/baileyrd/rusty_extract/pull/194), test `extract::plugin::tests` (file-resolution order only; C052/C182 cover consuming the resolved file) |
| C051 | `def/registry.ini` detector-to-plugin mapping (`[Trid]`/`[File]`/`[Exeinfo]` sections) — bridges detector string output to plugin-ini stems | behavior+config | code | none found (directory check only) | DONE | | PR [#196](https://github.com/baileyrd/rusty_extract/pull/196), test `detection::detector_mapping::tests` |
| C052 | `def/*.ini` plugin definition schema — `[Plugin]` section: display/executable/parameters/workingdir/runInTempOutdir/hide/log/patternSearch/initialShow/requireNetFramework/cleanup, with `%file%`/`%outdir%`/`%filename%`/`%tempoutdir%` placeholders | config | code | none found (directory check only) | DONE | | PR [#227](https://github.com/baileyrd/rusty_extract/pull/227), test `extract::plugin_config::tests` (schema parsing only; placeholder substitution is C182) |
| C053 | Manual disambiguation — when multiple candidate extractors exist for one format and automatic testing can't pick one, present the candidates for the caller to choose (GUI dialog itself is D001; the disambiguation *data*/policy is core) | behavior | code | none found (directory check only) | DONE | | PR [#378](https://github.com/baileyrd/rusty_extract/pull/378), tests `method_select::tests::override_wins_regardless_of_silent_mode`, `method_select::tests::silent_mode_without_override_auto_picks_first`, `method_select::tests::interactive_mode_without_override_prompts`, `method_select::tests::all_zero_override_is_treated_as_unset`, `method_select::tests::candidate_lists_match_source`; UniExtract.au3:7499 (GUI_MethodSelect), used at 2663,2705,2854,2893,3337 |
| C054 | Chained/composite recursive dispatch — a format's handling internally re-invokes the dispatcher with a different type for nested/container formats | behavior | code | none found (directory check only) | DONE | | **All 6 cited call sites' recursion piece now covered.** PR [#382](https://github.com/baileyrd/rusty_extract/pull/382) landed the shared completion contract (`extract::completion::resolve_completion`) plus 2362 `extract::actual`, 2543 `extract::forge`, 2999 `extract::raiu`; PR [#383](https://github.com/baileyrd/rusty_extract/pull/383) added 3385 `extract::zip`; PR [#384](https://github.com/baileyrd/rusty_extract/pull/384) added 2816 `extract::mscf`; PR [#385](https://github.com/baileyrd/rusty_extract/pull/385) completes 3173 `extract::unity` (already `DONE` as C121; only its recursive-dispatch piece was outstanding) — `extract($TYPE_7Z, -1, "gz", True, False)` shares `extract::forge`'s/`extract::raiu`'s `return_success=true, return_fail=false` shape, including the same `$outdir`-redirect-to-`$tempoutdir` dance `extract::forge` uses. **Scope note:** `$TYPE_MSCF`'s own fallback path (`RipExeInfo`, real Win32 GUI automation) remains out of scope under C069's existing blocker — that's `$TYPE_MSCF`'s own base-extractor behavior, not part of the recursive-dispatch mechanism C054 itself describes, which is now fully covered. Tests: `extract::completion::tests` (5), `extract::actual::tests::post_recursion_*` (3), `extract::zip::tests` (3), `extract::mscf::tests` (4), `extract::dispatch::tests::routes_zip_to_its_own_module`/`routes_mscf_to_its_own_module`, plus `extract::unity`'s pre-existing 5 (C121). UniExtract.au3:2362,2543,2816,2999,3173,3385 |
| C055 | Game-archive BMS-script lookup — SQLite-backed `BMS.db` mapping file extension → per-game QuickBMS script (150+ entries), separate from the `def/*.ini` mechanism | behavior+config | code | none found (directory check only) | DONE | | **The once-blocking `_SQLite_GetTable` array-shape question is now resolved against AutoIt's own official documentation, not guessed** (`$aResult[0]` holds `(rows+1)*columns`, not the row count; `$aResult[1..columns]` are headers; data follows row-major) — this unblocked both `CheckGame`'s lookup and `BmsExtract`'s own script fetch. PR [#410](https://github.com/baileyrd/rusty_extract/pull/410): new `bms` module ports `sql_lookup_outcome` (the row-count gate + `_ArraySort`, preserving why the source checks `> 1` not `> 0` — a header-deletion arithmetic artifact, not an off-by-one bug), `decide_game_choice` (`GUI_MethodSelectList`'s override/silent/prompt branching, including the override's list-position-1-means-"not a game" shift relative to C053's `GUI_MethodSelect`), `should_attempt_bms_extraction` (`BmsExtract`'s script-test classification), and the two literal SQL-query builders (preserving the source's own unescaped string-splice, not hardening it into a rewrite that would no longer match). Added `extract::qbms::gaup_probe_invocation`/`is_gaup_probe_failure` for `CheckGame`'s GAUP probe (also C180's own call site). Real SQLite access, the `.bms`-script write, and process termination stay out of scope, matching this crate's usual split; the actual QBMS extraction invocation reuses `extract::qbms::resolve_plugin_path`'s existing `$bms`-fallback case. Tests: `bms::tests` (18), `extract::qbms::tests::gaup_probe_*` (3). UniExtract.au3:1997-2049 (CheckGame), 3544-3569 (BmsExtract), 7561-7601 (GUI_MethodSelectList) |

| C056 | 7-Zip (`7z.exe`) integration — 7z/ZIP/GZIP/BZIP2/XZ/TAR/CPIO/AR/LZH/RPM/DEB/ARJ/LZMA/WIM/etc., plus the universal blind-probe fallback (C048); includes the 7z-SFX-stub splitter sub-step (`7ZSplit.exe`) | behavior | code | none found (directory check only) | DONE | | **Main extraction, PR [#388](https://github.com/baileyrd/rusty_extract/pull/388).** `extract::sevenzip` ports the main password-conditional invocation (reusing `password_search`, C160/C161), the `@error`/`@extended` → `MissingPart`/`Password` status classification, and the full RPM/Debian/gzip-family post-extraction branch tree. **The SFX-splitter branch is ported too, PR [#414](https://github.com/baileyrd/rusty_extract/pull/414):** `sfx_splitter_extract`, built on C069's `automation::GuiAutomation` infrastructure, launches `7ZSplit.exe`, clicks its two buttons, then polls (live `FileExists` interleaved with `WinExists` checks through the automation seam — a genuine exception to this crate's usual "filesystem checks are plain pre-computed arguments" split, since the loop's own exit condition needs it) for the expected script file or an error window, closing any warning window along the way; reports the found/target paths as data (`SfxSplitterOutcome`) rather than performing the final `_FileMove` itself. **Preserves the same no-timeout `WinWait` hang-risk quirk already found for C044's PEiD scan** (`WinWait("7z SFX Archives splitter")` passes no timeout, modeled as `u64::MAX`). Carries the same honesty caveat as C069: fake-backed tests prove the decision logic, not that the real Win32 backend drives an actual 7ZSplit window. Tests: `extract::sevenzip::tests` (22 total, including 5 in the new `sfx_splitter` submodule), plus `extract::dispatch::tests::routes_7z_to_its_own_module`. UniExtract.au3:2289-2343 |
| C057 | acefile (`acefile.exe`) — ACE archives, ACE SFX | behavior | code | none found (directory check only) | DONE | | PR [#206](https://github.com/baileyrd/rusty_extract/pull/206), test `extract::table::tests::ace_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C058 | AspackDie (`AspackDie.exe`) — ASPack-packed executables | behavior | code | none found (directory check only) | DONE | | PR [#221](https://github.com/baileyrd/rusty_extract/pull/221), test `extract::table::tests::aspack_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C059 | unalz (`unalz.exe`) — ALZip `.alz` archives | behavior | code | none found (directory check only) | DONE | | PR [#233](https://github.com/baileyrd/rusty_extract/pull/233), tests `detection::alz_probe::tests`, `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C060 | ARC (`arc.exe`) — `.arc` ARC-format archives | behavior | code | none found (directory check only) | DONE | | PR [#231](https://github.com/baileyrd/rusty_extract/pull/231), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C061 | ARJ SFX verification (`arj.exe`) — verify/list only, actual extraction delegates to 7-Zip (C056) | behavior | code | none found (directory check only) | DONE | | PR [#281](https://github.com/baileyrd/rusty_extract/pull/281), test `detection::arj_probe::tests::probe_invocation_matches_source` |
| C062 | BCM (`bcm.exe`) — `.bcm` BCM-compressed files | behavior | code | none found (directory check only) | DONE | | PR [#198](https://github.com/baileyrd/rusty_extract/pull/198), test `extract::table::tests::bcm_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C063 | bootimg (`bootimg.exe`) — Android boot images (`.img`) | behavior | code | none found (directory check only) | DONE | | PR [#283](https://github.com/baileyrd/rusty_extract/pull/283), test `extract::table::tests::bootimg_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C064 | Windows `expand.exe` — Microsoft CAB archives (`.cab`), MSU updates, including the self-extracting Type-1 CAB path | behavior | code | none found (directory check only) | DONE | | PR [#286](https://github.com/baileyrd/rusty_extract/pull/286), tests `extract::expand::tests::matches_source_invocation`, `extract::expand::tests::cab_self_extract_matches_source_invocation` |
| C065 | chdman (`chdman.exe`) — MAME CHD compressed hard disk images | behavior | code | none found (directory check only) | DONE | | PR [#199](https://github.com/baileyrd/rusty_extract/pull/199), test `extract::table::tests::chdman_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C066 | ci-extractor (`ci-extractor.exe`) — CreateInstall installers, GUI-automated | behavior | code | none found (directory check only) | DONE | | PR [#293](https://github.com/baileyrd/rusty_extract/pull/293), tests `extract::ci::tests::control_file_content_matches_source`, `extract::ci::tests::invocation_matches_source` |
| C067 | cicdec (`cicdec.exe`) — Clickteam Install Creator installers | behavior | code | none found (directory check only) | DONE | | PR [#208](https://github.com/baileyrd/rusty_extract/pull/208), test `extract::table::tests::cic_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C068 | GARbro (`GARbro.Console.exe`) — 500+ visual-novel/game-engine archive formats | behavior | code | none found (directory check only) | DONE | | PR [#205](https://github.com/baileyrd/rusty_extract/pull/205), test `extract::table::tests::garbro_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C069 | Exeinfo PE resource-extraction automation (`exeinfope.exe`) — GUI-automated resource ripping for Ghost Installer, MSCF, SWF-in-EXE, and some Wise/InstallShield MSI cache extraction (distinct from its detection use, C042) | behavior | code | none found (directory check only) | DONE | | **New GUI-automation infrastructure, PR [#411](https://github.com/baileyrd/rusty_extract/pull/411).** `automation::GuiAutomation` is a new trait covering the Win32 primitives `OpenExeInfo`/`RipExeInfo`/`CloseExeInfo` need (registry, process spawn, window wait/show/close, control click/send/get-text/get-handle, one listbox query, mouse/timer) — mirrors the `extract::runner::ExtractorRunner` split (real vs. fake implementation) already established for plain process spawning. `automation::open_exeinfo`/`close_exeinfo`/`rip_exeinfo` port the three functions themselves (UniExtract.au3:1822-1917) against this trait, reusing `detector_silence::EXEINFO_KEY`/`EXEINFO_SILENCE_VALUES`/`restore_plan` (C036) for the registry backup/restore rather than re-deriving it, and preserving the listbox-polling loop's two source quirks (both "End of file" spellings checked, timeout tested after both lookups each iteration). `automation::win32::Win32GuiAutomation` is a real, Windows-only (`#[cfg(windows)]`) implementation using the `windows` crate, type-checked against the `x86_64-pc-windows-gnu` target (this port's dev environment can't build native Windows binaries) — new small pure helpers `automation::keys`/`automation::control_spec` parse AutoIt's key-sequence and control-spec strings. **Honesty note, not glossed over:** `automation::fake::FakeGuiAutomation`-backed tests verify `open_exeinfo`/`close_exeinfo`/`rip_exeinfo`'s decision logic line-by-line against the source, the same confidence as every other parity test in this crate — but nothing proves `Win32GuiAutomation` actually drives a real Exeinfo PE window correctly, since no live interactive Windows desktop with the real tool exists in this environment or on CI (headless `windows-latest`). Tests: `automation::tests` (8), `automation::fake::tests` (6), `automation::keys::tests` (6), `automation::control_spec::tests` (6), `automation::win32::tests` (1, runs for real only on the `windows-latest` CI runner). UniExtract.au3:1861,1822,1897; used at 2568,2814,3100,2719,3357 |
| C070 | xor (`xor.exe`) — byte-XOR decode of Ghost Installer's overlay-extracted CAB blob | behavior | code | none found (directory check only) | DONE | | PR [#207](https://github.com/baileyrd/rusty_extract/pull/207), test `extract::table::tests::xor_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C071 | FreeArc (`unarc.exe`) — FreeArc `.arc` archives | behavior | code | none found (directory check only) | DONE | | PR [#200](https://github.com/baileyrd/rusty_extract/pull/200), test `extract::table::tests::freearc_matches_source_invocation` (consolidated into `extract::table` by PR [#362](https://github.com/baileyrd/rusty_extract/pull/362)) |
| C072 | FSB extractor (`fsbext.exe`) — FMOD Sample Bank (`.fsb`) | behavior | code | none found (directory check only) | DONE | | PR [#210](https://github.com/baileyrd/rusty_extract/pull/210), test `extract::table::tests::fsb_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C073 | helpdeco (`helpdeco.exe`) — Windows `.hlp` help files, with RTF reconstruction pass | behavior | code | none found (directory check only) | DONE | | PR [#352](https://github.com/baileyrd/rusty_extract/pull/352), tests `extract::helpdeco::tests::*` |
| C074 | innounp + innoextract (`innounp.exe`, `innoextract.exe`) — Inno Setup installers, GOG installers; primary/fallback pair | behavior | code | none found (directory check only) | DONE | | PR [#358](https://github.com/baileyrd/rusty_extract/pull/358), tests `extract::inno::tests::*` |
| C075 | InstallShield CAB fallback chain (`unshield.exe`, `i6comp.exe`, `i5comp.exe`, `iscab.exe`) — 4-tool chain, user/caller-selectable per C053 | behavior | code | none found (directory check only) | DONE | | PR [#379](https://github.com/baileyrd/rusty_extract/pull/379), tests in `extract::iscab::tests` (14 tests: invocation builders for `unshield`/`is6comp`/`is5comp`/`iscab`, `is6comp_file_count` parsing, retry/failure detection); UniExtract.au3:2655-2691 |
| C076 | IsXunpack (`IsXunpack.exe`) — legacy InstallShield installers | behavior | code | none found (directory check only) | DONE | | PR [#354](https://github.com/baileyrd/rusty_extract/pull/354), test `extract::table::tests::isxunpack_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C077 | QuickBMS (`quickbms.exe`) + WCX plugin fan-out (`gaup_pro.wcx`, `InstExpl.wcx`, `Iso.wcx`, `msi.wcx`, `TotalObserver.wcx`, `PDunSIS.wcx`) + `BMS.db`-selected `.bms` scripts (150+ game-specific) | behavior+config | code | none found (directory check only) | DONE | | **All 6 sites now covered.** PR [#389](https://github.com/baileyrd/rusty_extract/pull/389): `extract::qbms` ports the InstallExplorer/ISO/TotalObserver probe-then-classify detectors (2071, 2120, 2161), the shared `Case $TYPE_QBMS` extraction invocation and plugin-path resolution (2984), and the InstallExplorer-specific cleanup targets. PR [#410](https://github.com/baileyrd/rusty_extract/pull/410) completes the remaining two: `CheckGame`'s GAUP probe (2007, `extract::qbms::gaup_probe_invocation`/`is_gaup_probe_failure`) and `BmsExtract` itself (3544, the new `bms` module's `should_attempt_bms_extraction`/`bms_script_query`) — both were blocked on `_SQLite_GetTable`'s array-indexing semantics, resolved for C055/C180 against AutoIt's own official documentation rather than guessed. Tests: `extract::qbms::tests` (16 total), `bms::tests` (18). UniExtract.au3:2984,2007,2071,2120,2161,3544 |
| C078 | unisz (`unisz.exe`) — ISZ compressed ISO (stage 1 of disk-image conversion) | behavior | code | none found (directory check only) | DONE | | PR [#209](https://github.com/baileyrd/rusty_extract/pull/209), test `extract::table::tests::isz_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C079 | KGB Archiver (`kgb2_console.exe`) — `.kgb`/`.kge` archives | behavior | code | none found (directory check only) | DONE | | PR [#201](https://github.com/baileyrd/rusty_extract/pull/201), test `extract::table::tests::kgb_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C080 | lzip (`lzip.exe`) — `.lz` LZIP compressed files | behavior | code | none found (directory check only) | DONE | | PR [#202](https://github.com/baileyrd/rusty_extract/pull/202), test `extract::table::tests::lzip_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C081 | lzop (`lzop.exe`) — `.lzo` LZO compressed files | behavior | code | none found (directory check only) | DONE | | PR [#203](https://github.com/baileyrd/rusty_extract/pull/203), test `extract::table::tests::lzop_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C082 | unlzx (`unlzx.exe`) — `.lzx` LZX (Amiga) archives | behavior | code | none found (directory check only) | DONE | | PR [#223](https://github.com/baileyrd/rusty_extract/pull/223), test `extract::table::tests::lzx_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C083 | demoleition / MoleBox (`demoleition.exe`) — MoleBox-packaged executables | behavior | code | none found (directory check only) | DONE | | PR [#222](https://github.com/baileyrd/rusty_extract/pull/222), test `extract::table::tests::mole_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C084 | lessmsi (`lessmsi.exe`) — Windows Installer `.msi`, primary extractor | behavior | code | none found (directory check only) | DONE | | PR [#332](https://github.com/baileyrd/rusty_extract/pull/332), test `extract::table::tests::lessmsi_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C085 | jsMSIx (`jsMSIx.exe`) — MSI fallback method 1 | behavior | code | none found (directory check only) | DONE | | PR [#334](https://github.com/baileyrd/rusty_extract/pull/334), test `extract::table::tests::jsmsix_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C086 | MsiX (`MsiX.exe`) — MSI fallback 2, MSM merge modules, MSP patches | behavior | code | none found (directory check only) | DONE | | PR [#336](https://github.com/baileyrd/rusty_extract/pull/336), tests `extract::table::tests::msix_matches_source_invocation_without_ext`, `msix_matches_source_invocation_with_ext` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C087 | Windows `msiexec.exe` — MSI administrative-install fallback | behavior | code | none found (directory check only) | DONE | | PR [#338](https://github.com/baileyrd/rusty_extract/pull/338), test `extract::table::tests::msiexec_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C088 | NBHextract (`NBHextract.exe`) — HTC NBH ROM images | behavior | code | none found (directory check only) | DONE | | PR [#277](https://github.com/baileyrd/rusty_extract/pull/277), test `extract::table::tests::nbh_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C089 | Xpdf tools (`pdfdetach.exe`, `pdftohtml.exe`, `pdftopng.exe`, `pdftotext.exe`) — PDF content/attachment/text/image extraction, 4 sequential invocations | behavior | code | none found (directory check only) | DONE | | PR [#224](https://github.com/baileyrd/rusty_extract/pull/224), tests `extract::pdf::tests::detach_matches_source_invocation`, `to_html_matches_source_invocation`, `to_png_matches_source_invocation`, `to_text_matches_source_invocation` |
| C090 | PeaZip (`pea.exe`) — `.pea` PEA archives | behavior | code | none found (directory check only) | DONE | | PR [#370](https://github.com/baileyrd/rusty_extract/pull/370), test `extract::table::tests::pea_matches_source_invocation` |
| C091 | RAIU (`RAIU.exe`) — Reflexive Arcade Installer wrapper, chains into Inno Setup extraction (C074) | behavior | code | none found (directory check only) | DONE | | PR [#356](https://github.com/baileyrd/rusty_extract/pull/356), tests `extract::raiu::tests::*` |
| C092 | UnRAR (`UnRAR.exe`) — RAR archives, RAR SFX | behavior | code | none found (directory check only) | DONE | | PR [#330](https://github.com/baileyrd/rusty_extract/pull/330), tests `extract::table::tests::rar_matches_source_invocation_without_password`, `rar_matches_source_invocation_with_password` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C093 | RGSS Decryptor (`RgssDecrypter.exe`) — RPG Maker RGSS(2/3)A archives | behavior | code | none found (directory check only) | DONE | | PR [#186](https://github.com/baileyrd/rusty_extract/pull/186), test `extract::table::tests::rgss_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C094 | unrpa (`unrpa.exe`) — Ren'Py `.rpa` archives | behavior | code | none found (directory check only) | DONE | | PR [#190](https://github.com/baileyrd/rusty_extract/pull/190), test `extract::table::tests::rpa_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C095 | sfarkxtc (`sfarkxtc.exe`) — sfArk compressed SoundFont | behavior | code | none found (directory check only) | DONE | | PR [#193](https://github.com/baileyrd/rusty_extract/pull/193), test `extract::table::tests::sfark_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C096 | extsis (`extsis.exe`) — Symbian OS `.sis`/`.sisx` installers | behavior | code | none found (directory check only) | DONE | | PR [#195](https://github.com/baileyrd/rusty_extract/pull/195), test `extract::table::tests::extsis_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C097 | SQLite (`sqlite3.exe`) — SQLite database dump | behavior | code | none found (directory check only) | DONE | | PR [#276](https://github.com/baileyrd/rusty_extract/pull/276), test `extract::table::tests::sqlite_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C098 | swfextract (`swfextract.exe`) — Shockwave Flash (`.swf`) content extraction (sounds, images, streams) | behavior | code | none found (directory check only) | DONE | | PR [#373](https://github.com/baileyrd/rusty_extract/pull/373), test `extract::table::tests::swf_matches_source_invocation`. `-X -D` extracts every content type in one pass — a single invocation, not the per-target sequence this row's summary might suggest |
| C099 | Thinstall/ThinApp extractor (`Extractor.exe`) — virtualized executables, GUI-automated | behavior | code | none found (directory check only) | DONE | | PR [#376](https://github.com/baileyrd/rusty_extract/pull/376), tests `extract::thinapp::tests::matches_source_invocation`, `extract::thinapp::tests::relocated_file_path_matches_source_shape`; UniExtract.au3:3107-3128 |
| C100 | ttarchext (`ttarchext.exe`) — Telltale Games `.ttarch` archives, game selected via candidate list (C053) | behavior | code | none found (directory check only) | DONE | | PR [#348](https://github.com/baileyrd/rusty_extract/pull/348), test `extract::table::tests::ttarch_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C101 | UHARC 3-version fallback chain (`UNUHARC06.EXE`, `UHARC04.EXE`, `UHARC02.EXE`) — `.uha` archives | behavior | code | none found (directory check only) | DONE | | PR [#279](https://github.com/baileyrd/rusty_extract/pull/279), tests `extract::table::tests::uharc_matches_source_invocation`, `extract::table::tests::uharc04_matches_source_invocation`, `extract::table::tests::uharc02_matches_source_invocation` (consolidated into `extract::table` by PR [#362](https://github.com/baileyrd/rusty_extract/pull/362)) |
| C102 | uif2iso (`uif2iso.exe`) — MagicISO `.uif` images (stage-1 conversion to ISO) | behavior | code | none found (directory check only) | DONE | | PR [#211](https://github.com/baileyrd/rusty_extract/pull/211), test `extract::table::tests::uif_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C103 | umodel (`umodel.exe`/`unreal.exe`) — Unreal Engine packages | behavior | code | none found (directory check only) | DONE | | PR [#214](https://github.com/baileyrd/rusty_extract/pull/214), test `extract::table::tests::unreal_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C104 | ffmpeg (`ffmpeg.exe`) — audio conversion to WAV, video/audio track extraction from containers | behavior | code | none found (directory check only) | DONE | | **All three cited call sites now covered.** `extract::ffmpeg` already had `Case $TYPE_AUDIO`/`Case $TYPE_VIDEO_CONVERT`/the `$TYPE_VIDEO` probe invocation (earlier PR). This PR completes `$TYPE_VIDEO`'s per-stream extraction: `probe_output_unreadable`/`undercounted_stream_count`/`stream_segments`/`should_shortcut_to_plain_audio`/`parse_stream_segment`/`classify_stream`/`normalize_video_codec`/`normalize_audio_codec`/`build_stream_output_filename` plus 4 invocation builders. Preserves two genuine quirks: the `$iStreams` off-by-one (the WMA shortcut actually fires for up to two real streams, not fewer than two) and the dash-stripping asymmetry between `_MakeFFmpegCommand` and the image-sequence branch. One documented, accepted limitation: the header regex's `\d:\d` PCRE-backtracking edge case for double-digit-or-higher stream indices isn't byte-for-byte replicated. PR [#408](https://github.com/baileyrd/rusty_extract/pull/408), tests in `extract::ffmpeg::tests` (23 total). UniExtract.au3:2414,3216,3285 |
| C105 | VIS3Ext (`VIS3Ext.exe`) — Visionaire Engine v3 archives, two-pass invocation | behavior | code | none found (directory check only) | DONE | | PR [#350](https://github.com/baileyrd/rusty_extract/pull/350), tests `extract::visionaire3::tests::*` |
| C106 | Wise Installer 4-method fallback (`e_wise_w.exe`, `wun.exe`, self-extract switch, exeinfope MSI rip, `unzip.exe`) | behavior | code | none found (directory check only) | DONE | | **All 5 choices now covered.** PR [#409](https://github.com/baileyrd/rusty_extract/pull/409): `extract::wise` ports the primary `e_wise_w.exe` invocation, the primary-result routing (`classify_primary_result`), the five-choice `$iChoice` mapping (reusing `method_select::WISE_CANDIDATES`/`decide_method_selection`, C053, not duplicated), choices 1/2/4's invocation builders plus choice 1's cleanup-pattern data, and the completion-BAT invocation reached when the primary run didn't fail. **Choice 3 (`RipExeInfo` MSI-ripping) is ported too, PR [#415](https://github.com/baileyrd/rusty_extract/pull/415):** `wise_msi_rip`, built on C069's `automation::GuiAutomation` infrastructure, wraps `automation::rip_exeinfo` with `RIP_EXEINFO_KEY_SEQUENCE`. Carries the same honesty caveat as C069: fake-backed tests prove the delegation and its arguments, not that the real Win32 backend actually finds an MSI to rip. Tests: `extract::wise::tests` (16), plus `extract::dispatch::tests::routes_wise_to_its_own_module`. UniExtract.au3:3332-3371 |
| C107 | dark / WiX Toolset (`dark.exe`) — WiX MSI-based installers | behavior | code | none found (directory check only) | DONE | | PR [#213](https://github.com/baileyrd/rusty_extract/pull/213), test `extract::table::tests::wix_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C108 | WolfDec (`WolfDec.exe`) — Wolf RPG Editor game archives | behavior | code | none found (directory check only) | DONE | | PR [#215](https://github.com/baileyrd/rusty_extract/pull/215), test `extract::table::tests::wolf_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C109 | Info-ZIP UnZip (`unzip.exe`) — generic ZIP fallback when 7-Zip fails | behavior | code | none found (directory check only) | DONE | | PR [#218](https://github.com/baileyrd/rusty_extract/pull/218), test `extract::table::tests::unzip_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C110 | unzoo (`unzoo.exe`) — `.zoo` Zoo archives | behavior | code | none found (directory check only) | DONE | | PR [#217](https://github.com/baileyrd/rusty_extract/pull/217), test `extract::table::tests::zoo_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C111 | zpaq (`zpaq.exe`) — `.zpaq` ZPAQ archives | behavior | code | none found (directory check only) | DONE | | PR [#216](https://github.com/baileyrd/rusty_extract/pull/216), test `extract::table::tests::zpaq_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C112 | upx (`upx.exe`) — UPX-packed executables (unpack) | behavior | code | none found (directory check only) | DONE | | PR [#219](https://github.com/baileyrd/rusty_extract/pull/219), test `extract::table::tests::upx_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C113 | arc_conv (`arc_conv.exe`, UniExtract-authored) — KiriKiri/ERISA/YU-RIS engine archive conversion, GUI-automated | behavior | code | none found (directory check only) | DONE | | PR [#301](https://github.com/baileyrd/rusty_extract/pull/301), test `extract::table::tests::arc_conv_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C114 | Actual Installer inner-blob handling (reuses `unzip.exe` + `7z.exe`, plus embedded `aisetup.ini` rename manifest) | behavior | code | none found (directory check only) | DONE | | PR [#303](https://github.com/baileyrd/rusty_extract/pull/303), tests `extract::actual::tests::meta_invocation_matches_source`, `sanitize_drops_last_character_when_no_question_mark_present` |
| C115 | Advanced Installer self-extraction (native `/extract:` switch, no external binary) | behavior | code | none found (directory check only) | DONE | | PR [#305](https://github.com/baileyrd/rusty_extract/pull/305), test `extract::table::tests::ai_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C116 | Excelsior Installer self-extraction (native switches) | behavior | code | none found (directory check only) | DONE | | PR [#288](https://github.com/baileyrd/rusty_extract/pull/288), test `extract::table::tests::ei_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C117 | Netopsystems FEAD self-extraction (native switches) | behavior | code | none found (directory check only) | DONE | | PR [#289](https://github.com/baileyrd/rusty_extract/pull/289), test `extract::table::tests::fead_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C118 | SuperDAT Updater self-extraction (native switches) | behavior | code | none found (directory check only) | DONE | | PR [#291](https://github.com/baileyrd/rusty_extract/pull/291), test `extract::table::tests::superdat_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C119 | InstallForge (wraps `7z.exe` + base64 path-rename logic) | behavior | code | none found (directory check only) | DONE | | PR [#295](https://github.com/baileyrd/rusty_extract/pull/295), tests `extract::forge::tests::inner_archive_invocation_matches_source`, `decide_rename_decodes_valid_base64_name` |
| C120 | MSCF Cab installer (wraps `7z.exe` + Exeinfo-PE GUI rip) | behavior | code | none found (directory check only) | DONE | | PR [#297](https://github.com/baileyrd/rusty_extract/pull/297), test `extract::table::tests::mscf_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C121 | Unity `.unitypackage` decoder (wraps `7z.exe` + custom path-remapping logic) | behavior | code | none found (directory check only) | DONE | | PR [#299](https://github.com/baileyrd/rusty_extract/pull/299), tests `extract::unity::tests::inner_tar_invocation_matches_source`, `is_destination_within_outdir_accepts_contained_path` |
| C122 | unadf (`unadf.exe`) — Amiga Disk Format (`.adf`) images | behavior | code | none found (directory check only) | DONE | | PR [#231](https://github.com/baileyrd/rusty_extract/pull/231), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C123 | bitrock-unpacker (`bitrock-unpacker.exe`) — BitRock InstallBuilder installers | behavior | code | none found (directory check only) | DONE | | PR [#231](https://github.com/baileyrd/rusty_extract/pull/231), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C124 | BSA Browser (`bsab.exe`) — Bethesda Archive (`.bsa`/`.ba2`/`.pex`) | behavior | code | none found (directory check only) | DONE | | PR [#231](https://github.com/baileyrd/rusty_extract/pull/231), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C125 | godotdec (`godotdec.exe`) — Godot Engine packages (`.pck`, embedded in `.exe`) | behavior | code | none found (directory check only) | DONE | | PR [#231](https://github.com/baileyrd/rusty_extract/pull/231), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C126 | lbrate (`lbrate.exe`) — `.lbr`/`.lzr`/`.lqr` LBR archives | behavior | code | none found (directory check only) | DONE | | PR [#235](https://github.com/baileyrd/rusty_extract/pull/235), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C127 | ConvertLIT (`clit.exe`) — Microsoft Reader `.lit` eBooks. **Known quirk, verify still present**: `todo.txt` documents an output-path-with-spaces bug for this extractor. | behavior | code | none found (directory check only) | DONE | | PR [#235](https://github.com/baileyrd/rusty_extract/pull/235), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C128 | GNU gettext (`msgunfmt.exe`) — compiled GNU Gettext `.mo` message catalogs | behavior | code | none found (directory check only) | DONE | | PR [#235](https://github.com/baileyrd/rusty_extract/pull/235), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C129 | Champollion (`Champollion.exe`) — compiled Papyrus scripts (`.pex`, Bethesda) | behavior | code | none found (directory check only) | DONE | | PR [#235](https://github.com/baileyrd/rusty_extract/pull/235), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C130 | Qt Linguist (`lconvert.exe`) — Qt `.qm` compiled message translations | behavior | code | none found (directory check only) | DONE | | PR [#235](https://github.com/baileyrd/rusty_extract/pull/235), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C131 | rmvdec (`rmvdec.exe`) — RPG Maker MV encrypted resources (`.rpgmvp`/`.rpgmvo`/`.rpgmvm`) | behavior | code | none found (directory check only) | DONE | | PR [#235](https://github.com/baileyrd/rusty_extract/pull/235), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C132 | sgbdec (`sgbdec.exe`) — Smile Game Builder `.sgbpack` archives | behavior | code | none found (directory check only) | DONE | | PR [#235](https://github.com/baileyrd/rusty_extract/pull/235), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C133 | simdec (`simdec.exe`) — Smart Install Maker installers | behavior | code | none found (directory check only) | DONE | | PR [#235](https://github.com/baileyrd/rusty_extract/pull/235), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C134 | unar / TheUnarchiver (`unar.exe`) — StuffIt (`.sit`/`.sitx`) archives | behavior | code | none found (directory check only) | DONE | | PR [#235](https://github.com/baileyrd/rusty_extract/pull/235), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C135 | spoondec (`spoondec.exe`) — Spoon Installer/Streaming installers | behavior | code | none found (directory check only) | DONE | | PR [#235](https://github.com/baileyrd/rusty_extract/pull/235), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C136 | utagedec (`utagedec.exe`) — UTAGE visual novel engine files | behavior | code | none found (directory check only) | DONE | | PR [#235](https://github.com/baileyrd/rusty_extract/pull/235), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |
| C137 | UUDeview (`uudeview.exe`) — UUencode/yEnc encoded files | behavior | code | none found (directory check only) | DONE | | PR [#235](https://github.com/baileyrd/rusty_extract/pull/235), test `extract::plugin_defs_test::bundled_plugin_only_inis_produce_source_matching_command_lines` (consolidated into `extract::plugin_defs_test` by PR [#361](https://github.com/baileyrd/rusty_extract/pull/361)) |

| C138 | Output-subfolder default resolution — `/sub` resolves to a same-name subfolder, suffixed `_unpacked` if the input has no extension; falls back to an underscore-replaced name on collision | behavior | code | none found (directory check only) | DONE | | PR [#266](https://github.com/baileyrd/rusty_extract/pull/266), tests `outdir::tests::default_output_subfolder_single_extension`, `outdir::tests::default_output_subfolder_multi_extension_no_collision`, `outdir::tests::default_output_subfolder_multi_extension_collision_falls_back`, `outdir::tests::default_output_subfolder_no_extension_gets_suffix` |
| C139 | Output-directory relative-path resolution — relative/partial outdir paths resolve against the input file's directory; a leading single backslash inherits the input's drive letter | behavior | code | none found (directory check only) | DONE | | PR [#257](https://github.com/baileyrd/rusty_extract/pull/257), tests `outdir::tests::single_leading_backslash_inherits_drive_letter`, `outdir::tests::relative_path_resolves_against_filedir`, `outdir::tests::drive_absolute_and_unc_paths_pass_through` |
| C140 | Output-directory trailing-slash handling. **Known quirk, verify still present**: `ValidateOutputDirectory()` always appends a trailing `\`, `extract()` strips it immediately then re-appends only at function end, so mid-extraction code sees no trailing slash — todo.txt documents this as a known inconsistency. | behavior | code | none found (directory check only) | DONE | | PR [#257](https://github.com/baileyrd/rusty_extract/pull/257), test `outdir::tests::trailing_slash_normalized_to_backslash`; PR [#258](https://github.com/baileyrd/rusty_extract/pull/258), tests `outdir::tests::strip_trailing_backslash_matches_extract_start`, `outdir::tests::reappend_trailing_backslash_matches_extract_end` |
| C141 | Drive-root output directory behavior. **Known quirk, verify still present**: stripping the trailing `\` from a drive-root outdir (e.g. `C:\`→`C:`) causes the spawned extractor to resolve to Windows' ambiguous drive-relative cwd — reproduces the documented "extracting to C:/ creates a file in the script dir" bug. | behavior | code | none found (directory check only) | DONE | | PR [#260](https://github.com/baileyrd/rusty_extract/pull/260), test `outdir::tests::strip_trailing_backslash_reproduces_drive_root_ambiguity` |
| C142 | Output-directory creation and validation — create if missing (tracked for possible removal on failure); terminate with exit 5 if the path exists but isn't a writable directory | behavior | code | none found (directory check only) | DONE | | PR [#262](https://github.com/baileyrd/rusty_extract/pull/262), tests `outdir::tests::existing_writable_directory_is_already_valid`, `outdir::tests::missing_directory_created_successfully`, `outdir::tests::invalid_directory_cases_are_all_fatal` |
| C143 | No centralized overwrite policy. **Known quirk, verify still present**: overwrite behavior is fully delegated to each helper binary's own default; no global overwrite-all flag is injected anywhere. | behavior | code | none found (directory check only) | DONE | | PR [#372](https://github.com/baileyrd/rusty_extract/pull/372), test `extract::table::tests::no_format_injects_a_global_overwrite_flag` — sweeps all 48 `extract::table` formats in one pass |
| C144 | Overwrite/replace message treated as extraction success, not failure, in output-log evaluation | behavior | code | none found (directory check only) | DONE | | PR [#268](https://github.com/baileyrd/rusty_extract/pull/268), tests `log_eval::tests::recognizes_both_overwrite_substrings`, `log_eval::tests::does_not_match_unrelated_log_text` |
| C145 | Overwrite/password/no-space/new-filename prompt live-detection — streamed subprocess output is scanned for these substrings; on match, the extractor's window is force-shown for manual response (no auto-answer) | behavior | code | none found (directory check only) | DONE | | PR [#270](https://github.com/baileyrd/rusty_extract/pull/270), tests `log_eval::tests::recognizes_all_eight_prompt_substrings`, `log_eval::tests::matches_case_insensitively`, `log_eval::tests::does_not_match_ordinary_progress_output` |
| C146 | DAA→ISO conversion has no pre-existing-output-file check. **Known quirk, verify still present**: matches the documented "converting to iso fails when iso already exists" bug. | behavior | code | none found (directory check only) | DONE | | PR [#225](https://github.com/baileyrd/rusty_extract/pull/225), test `extract::table::tests::daa_matches_source_invocation` (consolidated into `extract::table` by PR [#360](https://github.com/baileyrd/rusty_extract/pull/360)) |
| C147 | Batch queue file format and duplicate handling — re-invocable command lines appended to a queue file; exact duplicates prompt the user; multipart archive volumes collapse to one queue entry | behavior | code | none found (directory check only) | DONE | | PR [#272](https://github.com/baileyrd/rusty_extract/pull/272), tests `batch::tests::build_command_line_matches_source_shapes`, `batch::tests::detects_queued_part_rar_volume`, `batch::tests::detects_queued_7z_volume`, `batch::tests::solo_rar_matches_via_ar_alternative_quirk`, `batch::tests::should_add_to_batch_matches_source_branches` |
| C148 | Batch-item-per-process execution model — each queued item spawns a brand-new process rather than looping in-process; chaining driven by the terminating status | behavior | code | none found (directory check only) | DONE | | PR [#342](https://github.com/baileyrd/rusty_extract/pull/342) (queue-array mechanics: PR [#274](https://github.com/baileyrd/rusty_extract/pull/274)), tests `batch_runner::tests::*`, `batch::tests::pop_batch_queue_*` |
| C149 | Batch stall on blocking user-input prompts. **Known quirk, verify still present**: if an extractor blocks on a modal prompt, the batch chain stalls indefinitely with no timeout — matches the documented bug. | behavior | code | none found (directory check only) | DONE | | **Verified still present.** `batch::should_continue_batch` already covered the 4235-4237 half (the `BatchQueuePop()` gate). `batch::needs_user_input` (new) ports the "needs user input" text match from the tee-log polling loop (4930-4933); confirmed the polling loop itself (`While ProcessExists($run) ... WEnd`, 4925-4958) has no timeout or give-up condition anywhere in it — an unattended run blocked on exactly the kind of prompt this function detects stalls indefinitely, matching the documented bug. All 8 literal needle strings verified present in the exact cited source range. PR [#404](https://github.com/baileyrd/rusty_extract/pull/404), tests in `batch::tests` (5 new). UniExtract.au3:4235-4237,4925-4958; todo.txt:54 |
| C150 | InstallShield-cab batch crash risk. **Known quirk, verify still present**: `is6cab.exe` still runs via blocking `RunWait` with no crash guard, matching the documented "might crash, stopping batch processing" bug. | behavior | code | none found (directory check only) | DONE | | PR [#380](https://github.com/baileyrd/rusty_extract/pull/380), test `extract::runner::tests::command_runner_blocks_until_exit_with_no_timeout_escape`; UniExtract.au3:2668-2674; todo.txt:27 |
| C151 | Batch-completion summary — on queue exhaustion, opens accumulated scan results (if any) and shows an error-log summary | behavior | code | none found (directory check only) | DONE | | `batch_runner::decide_batch_completion_actions` ports `BatchQueuePop()`'s "queue empty" branch — the sibling of `pop_and_relaunch_next_batch_item`'s "spawn next item" branch, which had already flagged this as the still-missing half. Also documents a real, easy-to-conflate distinction: this branch's keep-open relaunch (`$bOptKeepOpen` alone) is a different condition from `terminate()`'s own unrelated keep-open relaunch at UniExtract.au3:4238 (which also requires `$cmdline[0] = 0` and a non-silent status). PR [#405](https://github.com/baileyrd/rusty_extract/pull/405), tests in `batch_runner::tests` (4 new). UniExtract.au3:4451-4453 |
| C152 | Scan-only-mode short-circuit — bypasses the extraction dispatcher entirely, terminates with a file-info status | behavior | code | none found (directory check only) | DONE | | PR [#374](https://github.com/baileyrd/rusty_extract/pull/374), tests `entry_gate::tests::scan_only_gate_fires_when_not_extracting` (plus `scan_only_gate_does_not_fire_when_extracting`). Routing decision only; `FileScan_MediaInfo`'s scan is C045 (separate) |
| C153 | Scan-only full-detail output — concatenation of every scanner's results (TrID, Unix file, Exeinfo PE, PEiD, MediaInfo), not just a single type name | behavior | code | none found (directory check only) | DONE | | PR [#314](https://github.com/baileyrd/rusty_extract/pull/314), tests `filetype_report::tests::no_header_joins_type_text_only`, `header_centers_scanner_name_with_floor_division`, `header_floor_rounding_on_odd_remainder`, `header_name_longer_than_width_has_no_padding` |
| C154 | Scan-only silent-mode file output — results appended to the scan log file (path, blank line, scan text, separator), accumulated across a batch run | behavior | code | none found (directory check only) | DONE | | PR [#316](https://github.com/baileyrd/rusty_extract/pull/316), tests `filetype_report::tests::build_scan_log_entry_matches_source_format`, `build_scan_log_entry_separator_is_sixty_dashes` |
| C155 | Generic post-extraction cleanup utility — delete or move-to-subfolder modes, wildcard expansion, folder-vs-file handling, invoked per-extractor for installer cruft | behavior | code | none found (directory check only) | DONE | | PR [#381](https://github.com/baileyrd/rusty_extract/pull/381), tests in `cleanup::tests` (14 tests, including `split_wildcard_target_splits_at_last_backslash`, `split_wildcard_target_is_none_with_no_backslash`, `split_wildcard_target_is_none_when_backslash_is_first_character`); UniExtract.au3:3645-3703 |
| C156 | Per-run temp output directory always removed at end of extraction, success or failure | behavior | code | none found (directory check only) | DONE | | PR [#346](https://github.com/baileyrd/rusty_extract/pull/346), tests `outdir::tests::should_remove_temp_outdir_when_present`, `should_remove_temp_outdir_when_absent` |
| C157 | Freshly-created, still-empty output directory removed on extraction failure; non-empty failed output directories are left in place | behavior | code | none found (directory check only) | DONE | | PR [#264](https://github.com/baileyrd/rusty_extract/pull/264), tests `outdir::tests::empty_created_outdir_removed_on_failure`, `outdir::tests::nonempty_created_outdir_not_removed_on_failure`, `outdir::tests::preexisting_outdir_never_removed`, `outdir::tests::successful_run_never_removes_outdir` |
| C158 | Source-file deletion-on-success policy (keep/delete/ask/move), driven by C024; "ask" only prompts outside silent mode | behavior | code | none found (directory check only) | DONE | | PR [#241](https://github.com/baileyrd/rusty_extract/pull/241), test `prefs::tests::should_delete_source_file_matches_source_condition` |
| C159 | Unicode-relocation reversion at end of run — temp/ASCII working copies are moved back (or recycled) regardless of outcome | behavior | code | none found (directory check only) | DONE | | PR [#344](https://github.com/baileyrd/rusty_extract/pull/344), tests `unicode_relocation::tests::*` |
| C160 | Automated password-list trial — limited to 7z, DGCA, and RAR extraction; probes whether an archive is encrypted, then tries each password in the list until one succeeds or the list is exhausted | behavior | code | none found (directory check only) | DONE | | PR [#340](https://github.com/baileyrd/rusty_extract/pull/340), tests `password_search::tests::*` |
| C161 | ACE archives excluded from the password-list trial. **Known quirk, verify still present**: explicitly marked `TODO: _FindArchivePassword` in source; wrong/missing password just fails generically for this one extractor. | behavior | code | none found (directory check only) | DONE | | PR [#371](https://github.com/baileyrd/rusty_extract/pull/371), test `password_search::tests::ace_is_not_in_the_password_trial_set` (plus `password_trial_set_is_exactly_7z_dgca_and_rar`) |
| C162 | Generic password-failure detection via output-text matching, for extractor types with no automated list trial | behavior | code | none found (directory check only) | DONE | | PR [#321](https://github.com/baileyrd/rusty_extract/pull/321), tests `log_eval::tests::recognizes_all_five_whole_log_password_substrings`, `recognizes_enter_password_on_true_last_line`, `two_line_log_searches_whole_text_not_just_last_line` |
| C163 | Live password-prompt detection — "password" substring in streamed output force-shows the extractor's window as a manual-entry fallback | behavior | code | none found (directory check only) | DONE | | Already covered by C145 (PR [#270](https://github.com/baileyrd/rusty_extract/pull/270)) — UniExtract.au3:4931 is one line of the same 4930-4933 `Or`-chain `log_eval::needs_manual_input` fully ports; test `log_eval::tests::recognizes_all_eight_prompt_substrings` asserts the "password" case directly |
| C164 | Debug-line accumulation (timestamped, buffered in memory for the full run) | behavior | code | none found (directory check only) | DONE | | PR [#325](https://github.com/baileyrd/rusty_extract/pull/325), tests `run_log::tests::build_debug_line_matches_source_format`, `build_debug_line_with_empty_message` |
| C165 | Per-run log file naming/location/encoding — `<settingsdir>\log\YYYY-MM-DD_HH-MM-SS_[STATUS_]<filename>.<ext>.log`, UTF-16, status omitted from the filename on success | behavior | code | none found (directory check only) | DONE | | PR [#319](https://github.com/baileyrd/rusty_extract/pull/319), tests `run_log::tests::build_log_file_name_failed_run_includes_status_and_file`, `build_log_file_name_success_run_omits_status_marker`, `build_log_file_name_success_no_file_reproduces_trailing_underscore_quirk` |
| C166 | Teelog dual-output mechanism — subprocess stdout/stderr piped through a tee-like helper into a fixed live file, read incrementally for progress/prompt detection, folded into the run log and deleted at the end | behavior | code | none found (directory check only) | DONE | | **Decision logic, PR [#407](https://github.com/baileyrd/rusty_extract/pull/407).** `teelog::build_run_command` ports the tee-pipe command composition; `should_log_teelog_output` ports the fold-into-run-log gate; `decide_size_poll_action` ports the no-tee branch's own separate "reveal window once after 60s of no growth" heuristic, preserving a real quirk (`$bPatternSearch > -1` is a numeric comparison, not a boolean check — `0` and `1` both satisfy it). **Both branches are now fully orchestrated, PR [#417](https://github.com/baileyrd/rusty_extract/pull/417):** built on C069's `automation::GuiAutomation` infrastructure, extended with the process-polling/file-reading primitives this capability's own streaming needs introduced (`process_exists`/`win_get_by_pid`/`read_file_incremental`/`read_file_from_start`/`dir_size_bytes`/`win_set_state_by_title`/`win_activate`). `run_with_tee`/`decide_tee_iteration` port the tee branch's spawn-wait-poll-fold loop in full, including the exact `Do-Until` (body-first) shape of its three wait loops and `ContinueLoop`'s skip of the trailing `Sleep(100)` on a needs-input iteration; `run_without_tee` ports the no-tee branch's own spawn-hide-poll loop, reusing `decide_size_poll_action` unchanged. Reuses `batch::needs_user_input` (C149) for the live progress text scan and `log_eval::evaluate_log` (C167 family) for the final fold — neither duplicated here. **A genuine bug found and preserved, not fixed**: the tee branch's needs-input reveal calls `WinSetState($run, ...)` with the spawned process's **PID**, not `$runtitle` (the resolved window handle used two lines later) — a silent no-op in the source itself, reproduced via `win_set_state_by_title` on the stringified PID. **Not modeled**: `_PatternSearch`'s own 4-pattern regex-based progress parsing (taken as a caller-supplied closure, the same accepted limitation as `extract::ffmpeg`'s regex-backtracking edge case) and `_DirGetSize`'s big-drive-root guard (one shared byte count covers both of the source's slightly different call sites). Carries the same honesty caveat as C069: fake-backed tests prove the orchestration logic line-by-line against the source, not that the real Win32 backend drives a real spawned process's window correctly. Tests: `teelog::tests` (21). UniExtract.au3:4880-5008,4883,4960-4965; todo.txt:19,37 |
| C167 | Output-log evaluation and warning extraction — classifies success/failure/cancel/no-space/missing-part from tee output; extracts tool-specific warning blocks | behavior | code | none found (directory check only) | DONE | | PR [#323](https://github.com/baileyrd/rusty_extract/pull/323), tests `log_eval::tests::evaluate_log_classifies_each_outcome`, `evaluate_log_password_failure_takes_priority_over_success_text`, `parse_warnings_collects_multiple_and_none` |
| C168 | QuickBMS-specific interactive-prompt detection ("choose a new filename", "insert disk with") via the same live-output pattern match as C145/C163 | behavior | code | none found (directory check only) | DONE | | Already covered by C145 (PR [#270](https://github.com/baileyrd/rusty_extract/pull/270)) — UniExtract.au3:4932 is two more lines of the same 4930-4933 `Or`-chain `log_eval::needs_manual_input` fully ports; test `log_eval::tests::recognizes_all_eight_prompt_substrings` asserts both cases directly |
| C169 | Batch-mode error-log append — one line per non-zero-exit failure during silent/batch runs (timestamp, filename, status, extractor type) | behavior | code | none found (directory check only) | DONE | | PR [#308](https://github.com/baileyrd/rusty_extract/pull/308), tests `run_log::tests::should_append_error_log_requires_all_three_conditions`, `build_error_log_line_prefers_filenamefull_when_present` |
| C170 | Log-writing suppressed for certain terminal statuses (silent/syntax/non-silent-fileinfo/not-packed/batch) even when logging is enabled | behavior | code | none found (directory check only) | DONE | | PR [#308](https://github.com/baileyrd/rusty_extract/pull/308), tests `run_log::tests::should_save_log_suppresses_five_terminal_statuses`, `should_save_log_fileinfo_in_silent_mode_bypasses_other_gates` |
| C171 | Generic success/failure fallback heuristic — when an extractor case doesn't explicitly report success, infer it from output-directory size/mtime change relative to a pre-extraction snapshot | behavior | code | none found (directory check only) | DONE | | PR [#312](https://github.com/baileyrd/rusty_extract/pull/312), tests `result_heuristic::tests::no_size_growth_resolves_to_failed`, `unchanged_mtime_alone_resolves_to_failed`, `growth_and_mtime_change_treated_as_success`, `negative_one_initdirsize_skips_size_check`, `ace_exe_case_early_aborts_instead_of_failing` |
| C172 | Undifferentiated failure messaging. **Known quirk, verify still present**: the same generic failure prompt is shown regardless of total vs. partial failure — matches a documented open TODO. | behavior | code | none found (directory check only) | DONE | | PR [#377](https://github.com/baileyrd/rusty_extract/pull/377), tests `failure_message::tests::failure_message_key_matches_source_literal`, `failure_message::tests::failure_prompt_fires_when_not_silent`, `failure_message::tests::failure_prompt_suppressed_in_silent_mode`; UniExtract.au3:4195; todo.txt:48 |
| C173 | Batch continues past an ordinary (clean-exit) per-item failure — only a hang/crash stalls the chain, not a normal failed extraction | behavior | code | none found (directory check only) | DONE | | PR [#310](https://github.com/baileyrd/rusty_extract/pull/310), tests `batch::tests::should_continue_batch_continues_on_ordinary_statuses`, `should_continue_batch_stops_on_silent_status`, `should_continue_batch_stops_when_batch_disabled` |
| C174 | Per-extractor timeout handling (implemented case-by-case, not globally; most cases have none) | behavior | code | none found (directory check only) | DONE | | **Verified**: `$Timeout` is referenced from ~15 scattered call sites across the whole source, out of ~70 extractor `Case`s — confirming no global mechanism exists. `extractor_timeout::arc_conv_timeout_outcome` ports the representative case's decision (`WinWait`'s zero-handle return means timeout, terminating with `$STATUS_TIMEOUT`); `WinWait` itself stays out of scope (GUI, row D001). Most other `$Timeout` sites just `ExitLoop` a polling loop without terminating — a genuinely different, non-uniform behavior per site, not a shared mechanism to generalize. PR [#406](https://github.com/baileyrd/rusty_extract/pull/406), tests in `extractor_timeout::tests` (3). UniExtract.au3:2398-2399 (representative case) |
| C175 | Non-ASCII filename/path relocation before processing — rename in place for a non-ASCII filename, copy/move to a temp dir for a non-ASCII directory (aborts with a warning if the temp dir itself is non-ASCII); multipart archive members are exempted | behavior | code | none found (directory check only) | DONE | | `unicode_relocation::plan_relocation` ports `MoveInputFileIfNecessary`'s full destination decision, plus `is_allowed_charset` (`$sRegExAscii`, verified to whitelist 20 accented Western-European letters, both cases, not just ASCII — extracted programmatically from the source's own `\Q...\E` block), `is_multipart_exempt`, `decide_relocation_mode` (Move-vs-Copy), and `should_reset_outdir`. `ẞ` (U+1E9E)'s case-folding is a documented, deliberately-unresolved minor uncertainty. PR [#400](https://github.com/baileyrd/rusty_extract/pull/400), tests in `unicode_relocation::tests` (20 new, 23 total). UniExtract.au3:2218-2266 |
| C176 | UNC-path input relocation — files reached via a UNC path are unconditionally relocated to a temp directory before processing | behavior | code | none found (directory check only) | DONE | | Covered by the same `unicode_relocation::plan_relocation` port as C175 (same function, same PR [#400](https://github.com/baileyrd/rusty_extract/pull/400)). Preserves a real interaction between the two capabilities: UNC relocation only supplies its own destination when the unicode check didn't already compute one — a file that's both unicode-named and UNC-reached uses the unicode branch's destination, not a separate UNC one. UniExtract.au3:2219,2236-2237 |
| C177 | Unicode-move bookkeeping lost on nested re-entry. **Known quirk, verify still present**: a recursive re-scan (e.g. after unpacking a self-extracting packer) resets unicode-mode state, discarding the outer run's relocation bookkeeping mid-flight. | behavior | code | none found (directory check only) | DONE | | **Verified still present** against a directly downloaded copy of the raw source: `unpack()`'s post-unpack re-scan (UniExtract.au3:3633-3635) calls `StartExtraction()` directly, whose first statement (line 378, unconditional on every entry) resets `$iUnicodeMode`. `unicode_relocation::start_extraction_reentry_resets_unicode_mode` documents this precisely and makes it testable — a known, not-yet-fixed bug in the source, preserved rather than "fixed" under this migration's parity contract. PR [#401](https://github.com/baileyrd/rusty_extract/pull/401), test `unicode_relocation::tests::nested_start_extraction_reentry_discards_outer_unicode_mode`. UniExtract.au3:378,3603-3642,3633-3636; todo.txt:26 |
| C178 | TrID UNC-path detection reliability. **Known quirk, verify still present**: the TrID DLL load call uses ANSI string marshalling, consistent with the documented UNC-path detection-failure report. | behavior | code | none found (directory check only) | DONE | | **Verified still present**: `TridLib_Load`'s `TrID_LoadDefsPack` call (line 949) and `TridLib_Analyse`'s `TrID_SubmitFileA` call (line 964, note the "A"/ANSI Win32 suffix) both marshal their string arguments as `"str"` (ANSI) — confirmed no `"wstr"` call site exists anywhere in the wrapper. `detection::trid_scan::trid_dll_string_marshalling` makes the finding explicit and testable. PR [#402](https://github.com/baileyrd/rusty_extract/pull/402), test `detection::trid_scan::tests::trid_dll_calls_use_ansi_not_wide_string_marshalling`. UniExtract.au3:944-957,949; todo.txt:28 |
| C179 | Free-space check blocks unattended continuation — insufficient space terminates immediately in silent mode; interactive mode offers abort/retry/ignore | behavior | code | none found (directory check only) | REQUIRED | | **Scope — the `MsgBox` call itself stays unmodeled.** `free_space::measure_free_space`/`decide_free_space_outcome` port the arithmetic and silent-vs-interactive branch (PR [#318](https://github.com/baileyrd/rusty_extract/pull/318), noting the distinct `$STATUS_FAILED`-not-`$STATUS_NOFREESPACE` exit-status quirk on the silent-mode path). `free_space::decide_prompt_action` (PR [#403](https://github.com/baileyrd/rusty_extract/pull/403)) adds the Retry/Abort/Ignore response handling once a response is obtained — including the finding that Ignore (and any unexpected `MsgBox` return value) has no explicit `Case`, silently falling through to continue extraction despite insufficient space. Tests: `free_space::tests` (13). UniExtract.au3:3782-3808 |
| C180 | QuickBMS game-listing hang risk. **Known quirk, verify still present**: the blocking game-database listing call used during detection (not just extraction) has no timeout, consistent with a documented hang-with-high-CPU report. | behavior | code | none found (directory check only) | DONE | | **Traced fully, resolves to two already-`DONE` findings, not a new mechanism.** The GAUP probe (UniExtract.au3:2007) isn't literally unbounded — `FetchStdout`'s own polling loop (UniExtract.au3:5075-5098) bounds it by `$Timeout` — but an unset/first-run `$Timeout` preference resolves to ~16.7 hours (`prefs::tests::missing_preference_key_reproduces_the_sixty_million_millisecond_quirk`, C026), and the loop busy-polls (1ms `Sleep`) the whole span rather than blocking efficiently: "hang with high CPU" is that busy-poll, not a true infinite hang. This crate's own runner (`extract::runner::CommandExtractorRunner`) doesn't reproduce the busy-poll shape and carries no timeout modeling for any call site regardless (C150, PR [#380](https://github.com/baileyrd/rusty_extract/pull/380)) — so there's no separate mechanism left to port under this ID. PR [#410](https://github.com/baileyrd/rusty_extract/pull/410) additionally gives this exact call site its own invocation builder (`extract::qbms::gaup_probe_invocation`), landed together with C055 since it's `CheckGame`'s own GAUP probe. UniExtract.au3:1997-2049,2007; todo.txt:55 |
| C181 | Multi-stage/recursive extraction for nested container formats — several extractor types re-invoke the dispatcher on their own intermediate output | behavior | code | none found (directory check only) | DONE | | **All 3 cited ranges now covered.** 2351-2383 (`$TYPE_ACTUAL`, `extract::actual`) and 2994-3001 (`$TYPE_RAI`, `extract::raiu`) via `extract::completion`, PR [#382](https://github.com/baileyrd/rusty_extract/pull/382). The third, 2477-2497 (`$TYPE_CTAR`), turned out to be a genuinely different mechanism — it loops re-invoking `7z` directly on each newly-discovered nested archive, never calling `extract()` recursively at all — ported separately in `extract::ctar`, PR [#386](https://github.com/baileyrd/rusty_extract/pull/386), tests in `extract::ctar::tests` (8) plus `extract::dispatch::tests::routes_ctar_to_its_own_module`. Preserves a real quirk: the old-files check (`ReturnFiles`'s pipe-delimited snapshot vs. a bare `StringInStr`) is a substring match, not an exact-token comparison. UniExtract.au3:2351-2383,2477-2497,2994-3001 |
| C182 | Plugin extension point placeholder substitution (`%filename%`, `%fileext%`, `%filedir%`, `%file%`, `%outdir%`, translation-key placeholders) for `def/*.ini`-declared extractors | behavior | code | none found (directory check only) | DONE | | PR [#229](https://github.com/baileyrd/rusty_extract/pull/229), test `extract::placeholder::tests` |
| C183 | Main window creation, layout, and DPI/theme adaptation — base window, menus, radio/combo/input controls, corner-rounding, DPI-aware scaling, light/dark + high-contrast detection, min-size enforcement, position/size persistence | interface | code | none found (directory check only) | REQUIRED | | **Partial, PR [#454](https://github.com/baileyrd/rusty_extract/pull/454).** New `gui` module (`egui`/`eframe`, a new dependency, `#[cfg(windows)]`-gated same as `automation`/`dlllib`): `gui::layout` ports `GetPos`'s control-chaining math including the undocumented RTL 0.4 X-offset factor, the ex-style `-1`-sentinel resolution, and the "minimum size is the measured-after-creation size, not the nominal `GUICreate` size" contract; `gui::theme` ports `_AppsUseLightTheme` (reusing `automation::GuiAutomation::reg_read_dword`, C069 — no new registry-read path needed), `_IsHighContrastMode`'s bit test, and `_GuiSetColor`'s three-way white-background gate; `gui::window_state` ports `GUI_SavePosition`'s exists-and-preference-on gate. `gui::app::MainWindow` is a real, compiling `eframe::App` wiring the window shell (File/Edit/Help menus, Extract/Scan-only selector, file/output-dir fields, OK/Cancel/Batch buttons) and running real theme/high-contrast/Windows-version detection at startup via `SystemParametersInfoW`/`RtlGetVersion`; `main()` now launches it on zero CLI args, matching `ParseCommandLine`'s own `$prompt = True` branch. **Not yet done, stays `REQUIRED`**: real position/size persistence (D010's `storeguiposition`/`posx`/`posy`/`GuiWidth`/`GuiHeight` read/write — only the save *gate* is ported, not the read/write itself), the DPI-scaling engine (`_GuiSetScale`), corner-rounding (`_GuiRoundCorners`), the GDI+ logo-image loader (`_GDIPlus_LoadImage`), the disabled-control tooltip workaround (`GUI_Create_Tooltip` — likely obsolete under egui's native tooltip-on-disabled support, a design call for whoever finishes this row to confirm rather than silently drop), and literal widget-for-widget fidelity to `CreateGUI`'s exact control set (the file-history combo-vs-plain-input choice, real menu-item click handlers — those wire up as their own capabilities land, C184-C217). Carries the same honesty caveat as C069/C038/C045/C166: fake-backed/pure-function tests prove the decision logic, real interactive verification needs CI's `windows-latest` runner and a real Windows machine. Tests: `gui::layout::tests` (6), `gui::theme::tests` (4), `gui::window_state::tests` (3). Quirks preserved: `_AppsUseLightTheme` fails open to light on registry-read error while `_IsHighContrastMode` fails closed to false (deliberately inconsistent polarity, both kept); `_GuiSetColor`'s white-background override is Windows-10-only (not applied on 11). UniExtract.au3:5806-5980,6036-6202,6988-7038,8107-8149 |
| C184 | System tray icon lifecycle and menu — tray icon creation, right-click menu (hide-status toggle + Exit), click-mode configuration, icon-hide preference | interface | code | none found (directory check only) | REQUIRED | | **Partial, PR [#455](https://github.com/baileyrd/rusty_extract/pull/455).** `gui::tray` ports the pure decisions: `hide_status_item_checked`/`should_hide_icon` (both direct preference mirrors), `decide_console_visibility_action` (`Tray_ShowHide`'s no-op-if-gone / toggle-if-running logic), and `should_log_tray_exit` (`Tray_Exit`'s `SaveLog($STATUS_TRAYEXIT)` firing only for a pure-tray/silent session, not when exiting the tray with the main window still open). `gui::tray_icon_shell::TrayHandle` is a real tray icon + two-item menu ("Hide status" checkbox, "Exit") via the `tray-icon` crate (a new dependency — `eframe`/`egui` has no tray-icon primitive of its own, and hand-rolling `Shell_NotifyIconW` would have to cooperate with `winit`'s already-owned event loop rather than run its own), polled once per frame from `gui::app::MainWindow::update`. **Verified against AutoIt's own docs, not assumed**: `TraySetClick(8)` is `$TRAY_CLICK_SECONDARYDOWN` — the menu opens on right-click-down only, nothing bound to left-click — which is `tray-icon`'s own default Windows-backend behavior already, so no explicit click-mode configuration was needed. **Not yet done, stays `REQUIRED`**: `Tray_ShowHide`'s console-visibility action isn't wired to a real spawned helper process yet (no extraction is launched from the GUI at all so far); `Tray_Exit`'s real sequence (`KillHelper` + `GUI_SavePosition` + the conditional status log) isn't wired to the Exit menu item, which currently only closes the window. Carries the same honesty caveat as C069/C183: fake-backed/pure-function tests prove the decision logic; nothing proves the real tray icon renders or responds correctly, since no interactive Windows desktop exists in this environment or on headless `windows-latest` CI. Tests: `gui::tray::tests` (5). UniExtract.au3:8149-8197 |
| C185 | Tray status/progress popup window lifecycle — its own window creation/positioning (relative to taskbar), fade-in/fade-out animation, dark/light background, corner-rounding, and the tray-menu hide/show toggle + `nostatusbox`/`hidestatusboxiffullscreen` preference persistence. Distinct from C166, which only ported the *text-update* call into an already-existing popup. | interface | code | none found (directory check only) | REQUIRED | | **Partial, PR [#456](https://github.com/baileyrd/rusty_extract/pull/456).** `gui::tray_status_box` ports the pure decisions: `should_show_status_box`/`should_suppress_for_fullscreen` (both gates, with the fullscreen check an *exact* active-window-size-equals-desktop-size match, not a fuzzy "mostly covers the screen" heuristic), `truncate_with_ellipsis` (28-char filename / 56-char message caps, character-counted not byte-counted so a multi-byte character is never split), and `resolve_position` — including the real quirk that the source detects a top-docked taskbar via `$pos[0] = $pos[1]` (X equals Y), which only actually holds at the origin `(0,0)`, ported as the same equality test rather than "clarified" into an explicit Y-is-zero check, plus the out-of-range-falls-back-to-bottom-right clamp. `gui::app::MainWindow` renders a real popup via `egui`'s multi-viewport support (`show_viewport_immediate` — undecorated, transparent, always-on-top, no taskbar entry), wired to real `GetSystemMetrics`/`GetForegroundWindow`+`GetWindowRect`/`FindWindowW("Shell_TrayWnd")`+`GetWindowRect` calls for the desktop/active-window/taskbar geometry `gui::tray_status_box`'s pure functions need. Preserves the source's hardcoded-dark-always background (`$bDark = True` — this popup is never light-themed, unlike the main window's C183 theme detection) and corner-rounding (via `egui`'s native `corner_radius`, achieving the same visual result as `_GuiRoundCorners`'s Win32 region approach without replicating that specific mechanism, the same class of idiomatic adaptation as C183's DPI-scaling note). **Fade-in is animated** (frame-timed alpha over the same ~230ms the source's 23-step `Sleep`-loop takes) — **fade-out is not** (the popup is simply removed), a documented simplification. **Not yet done, stays `REQUIRED`**: preference persistence for `nostatusbox`/`hidestatusboxiffullscreen`/`statusposx`/`statusposy` (C184 already wires the tray-menu toggle to in-memory state; nothing yet reads/writes these to the real prefs file — the same category of remaining gap as C183's own position-persistence note); fade-out animation. Carries the same honesty caveat as C069/C183/C184. Tests: `gui::tray_status_box::tests` (9). UniExtract.au3:4261-4342,8175-8190 |
| C186 | File/directory input, validation, and output-directory auto-fill — file/dir text fields, native file-open/folder-picker dialogs, the `FilenameParse`-gated validate-and-commit path (`/sub` sentinel default), and the directory-auto-fill-unless-locked rule | interface+behavior | code | none found (directory check only) | REQUIRED | | **Partial, PR [#457](https://github.com/baileyrd/rusty_extract/pull/457).** `gui::file_input` ports the pure decisions: `is_blank`/`parse_filename` (`FilenameParse`'s parsing half, reusing `outdir::split_file_path`/`outdir::default_output_subfolder` — C138 — for `$initoutdir` rather than re-deriving its multi-extension collision-avoidance quirk), `decide_ok_set` (`GUI_OK_Set`'s validate-then-resolve-`$outdir` path, `/sub` sentinel on a blank directory field), `should_auto_fill_on_file_input_changed` (`GUI_OnFileInputChanged`'s simpler blank-only gate, fired on every keystroke) vs. `should_auto_fill_output_dir` (`GUI_Drop_Parse`'s own fuller OR gate — blank *or* unlocked, reusing `prefs::KEEPOUTPUTDIR_DEFAULT` — C027 — for the lock preference's default rather than re-deriving it), `parse_file_dialog_result` (the multi-select pipe-string format), and `resolve_folder_picker_seed` (`GUI_Directory`'s fallback derivation). `gui::app::MainWindow` wires real native file-open/folder-picker dialogs via a new dependency, the `rfd` crate (the closest real-API equivalent of `FileOpenDialog`/`FileSelectFolder`, cross-platform-buildable so it also type-checks on this dev environment's own host target, not just `x86_64-pc-windows-gnu`), the OK button's real validate-and-close path, and live auto-fill as the file field is typed into or a file is picked. Also moved `main`'s own private `split_file_path` helper into `outdir` as a public function (`outdir::split_file_path`) so both `main`'s CLI composition root and this new GUI code share one implementation instead of two independently-derived copies. **Single-file selection only for now** — the dialog's multi-select path routes into `GUI_Drop`'s populate-vs-auto-queue dispatch (C187) and potentially the batch queue (C188), neither of which exist yet; wiring multi-select today with nowhere for a 2nd+ file to go would silently drop every file but the first. **Not yet done, stays `REQUIRED`**: the "invalid file" `MsgBox` isn't wired (C194's own row) so an invalid OK click is currently silent rather than explained; `EnvParse`'s `%VAR%` env-variable expansion isn't applied to either field yet. Carries the same honesty caveat as C069/C183/C184/C185 for the real-dialog wiring specifically (the file-open/folder-picker dialogs themselves can't be interactively verified in this environment). Tests: `gui::file_input::tests` (14), `outdir::tests::split_file_path_separates_dir_stem_and_extension` (moved from `main.rs`'s own test suite). UniExtract.au3:500-523,6264-6303,6555-6585,6735-6756 |
| C187 | Drag-and-drop file/directory handling — `WM_DROPFILES` multi-file enumeration and the central drop dispatcher deciding populate-only vs. auto-queue vs. recurse-into-directory | behavior | code | none found (directory check only) | REQUIRED | | **Partial, PR [#458](https://github.com/baileyrd/rusty_extract/pull/458).** `gui::drag_drop` ports `GUI_Drop`'s full per-item dispatch (`decide_drop_action`/`decide_drop_actions`): a nonexistent path is silently skipped; a directory always expands into the batch queue (C188) regardless of batch size; a file is `PopulateOnly` when it's the *sole* dropped path (fields populated, nothing auto-queued) or `PopulateAndQueue` when it's one of several (populated *and* queued via C188). **`WM_DROPFILES_UNICODE_FUNC` is not ported as its own piece** — it's a raw `DragQueryFileW` enumeration loop whose entire job (turning an OS drop event into a path list) is already done by `egui`'s own native drag-drop input (`ctx.input(|i| i.raw.dropped_files)`), which supersedes the Win32 workaround rather than needing a parallel implementation — the same class of "old workaround made moot by the new toolkit" as C183's DPI-scaling note and C185's tooltip-workaround note. `gui::app::MainWindow` wires this for real via `eframe`'s native drop-file input, with `NativeOptions`'s `with_drag_and_drop(true)` matching the source's own `$WS_EX_ACCEPTFILES` window ex-style. **Only the single-file (`PopulateOnly`) case is wired for real** — a multi-file drop or a dropped directory routes into the batch queue (C188), which doesn't exist yet; silently keeping only the last of several dropped files would be a worse outcome than doing nothing, the same call already made for C186's multi-select scope note. Carries the same honesty caveat as C069/C183-C186 for the real drop-handling wiring. Tests: `gui::drag_drop::tests` (6). UniExtract.au3:6202-6219,6710-6735 |
| C188 | Batch queue management UI — add-to-queue/run dual-meaning button, queue finalize-and-execute, recursive directory expansion into the queue, and the queue-edit dialog (list/delete/save-or-cancel) | interface+behavior | code | none found (directory check only) | REQUIRED | | **Partial, PR [#459](https://github.com/baileyrd/rusty_extract/pull/459).** `gui::batch_queue` ports the GUI-layer decisions on top of the already-DONE C147/C148 queue mechanics (`crate::batch::should_add_to_batch`/`build_command_line`, reused rather than re-derived): `decide_batch_button_action` (`GUI_Batch`'s overloaded button — "Add" when the current fields validate, "Run" when they don't but the queue already has items, error only when both are empty), `should_clear_output_dir_on_batch_add`, `resolve_batch_recurse` (the `BatchRecurse` clamp-above-1 quirk), `list_directory_files` (real, unsorted — `$FLTAR_NOSORT` — recursive enumeration, unfiltered glob), `should_disable_batch_mode_on_ok`, `delete_queue_item`, and `should_show_full_text_tooltip`. `gui::app::MainWindow` wires an in-memory `batch_queue: Vec<String>` for real: the Batch button's Add branch, and — completing the gap C187 explicitly left open — drag-and-drop's `AddDirectory`/`PopulateAndQueue` cases (a dropped directory recursively queues every file found; one of several dropped files is populated then immediately queued, matching the source's own per-item populate-then-`GUI_Batch()` loop). **Not yet done, stays `REQUIRED`**: the Batch button's "Run" branch (real batch execution needs `GUI_Batch_OK`'s `terminate`+`crate::batch_runner` relaunch chain, which needs a real extractor invocation the GUI can't build yet — no detection cascade, C037-046, is wired into the GUI); the queue-edit dialog (`GUI_Batch_Show`) itself has no real window yet, only its pure decisions (`should_disable_batch_mode_on_ok`/`delete_queue_item`/`should_show_full_text_tooltip`) are ported; the queue lives in memory only, not persisted to the real `$batchQueue` file; the exact-duplicate `CustomPrompt('BATCH_DUPLICATE', ...)` dialog (C193) isn't wired, so an exact duplicate is always silently skipped rather than prompted; `BatchRecurse` is hardcoded to the default-on value rather than read from real prefs (the same category of gap as C183/C185's own persistence notes). Carries the same honesty caveat as C069/C183-C187. Tests: `gui::batch_queue::tests` (11, including two real-filesystem tests against a temp directory). UniExtract.au3:6585-6636,6636-6710 |
| C189 | Pre-execution warning gate — the "you are about to run an executable" confirmation before launching a file found inside/derived from an archive, with a persisted "don't ask again" preference | interface+behavior | code | none found (directory check only) | REQUIRED | | **Partial, PR [#460](https://github.com/baileyrd/rusty_extract/pull/460).** `warn_execute::decide_warn_execute_outcome` ports `Warn_Execute`'s dispatch (warning disabled skips the dialog and always proceeds; a shown-and-declined dialog aborts), mirroring `free_space::decide_prompt_action`'s (C179) `AbortAndTerminateSilently { remove_created_outdir }` shape for the same `$createdir` gate — cleans up only a temp output directory this run itself created, never a pre-existing one. `should_disable_warn_execute_permanently` ports the "don't ask again" checkbox's persistence. **Verified, not fixed**: `GUI_Warn_Execute` never restores `Opt("GUIOnEventMode")` back to 1 after its dialog closes (sets it to 0 again instead), unlike `GUI_Batch_Show`'s correct restore — a real copy-paste-looking bug, but moot for this port since `egui`'s immediate-mode loop has no equivalent global event-dispatch toggle to get wrong. **Not yet done, stays `REQUIRED`**: the real confirm/cancel dialog itself isn't wired — every one of `Warn_Execute`'s ~13 call sites sits inside the extraction dispatch table, which this port's GUI doesn't drive at all yet (no detection cascade, C037-046, wired in — the same gap already blocking C188's Batch "Run" branch and the OK button's real extraction in C183/C186); a dialog with no caller that could trigger it would be dead code, not a meaningful port. Tests: `warn_execute::tests` (4). UniExtract.au3:6219-6264 |
| C190 | Preferences dialog and instant-apply tray toggles — the full settings dialog (language, update interval, source-deletion policy, ~13 behavior checkboxes) plus four standalone instant-apply tray-menu toggles (scan-only, silent, keep-open, topmost) | interface+behavior+config | code | none found (directory check only) | REQUIRED | | **Partial, PR [#461](https://github.com/baileyrd/rusty_extract/pull/461).** `gui::prefs_dialog` ports the real quirks inside `GUI_Prefs_OK`/the standalone toggles: `decide_history_toggle_outcome` (unchecking "remember history" *deletes* the two history INI keys outright, not just zeroes a flag), `resolve_update_interval_display_index`/`resolve_update_interval_value` (the fixed preset table — 1/7/30/365/999999/custom — including the "Custom" selection keeping whatever was already stored rather than accepting a typed value; folds in D003's `iOptUpdateInterval`), `decide_send_stats_command`/`should_check_update_after_nightly_toggle` (both only fire their network side effect — `SendStats`/`CheckUpdate` — on an actual change, not on every save), `resolve_checked_delete_source_file_radio` (reusing `prefs::parse_delete_source_file_option`, C024), `should_persist_scan_only_pref` (`GUI_ScanOnly`'s startup-vs-user-click save-skip parameter), and `resolve_topmost_ex_style`. `gui::app::MainWindow` wires two pieces for real: a "Lock output directory" checkbox (`GUI_KeepOutdir`, mirroring the already-consumed `lock_output_directory` field from C186) and "Always on top" (`GUI_Topmost`) via `egui`'s `ViewportCommand::WindowLevel` — applied live on the existing viewport rather than replicating the source's own destroy-and-recreate-the-window workaround (`WS_EX_TOPMOST` can't be changed on a live Win32 window handle; `egui` has no such limitation). **Not yet done, stays `REQUIRED`**: the Preferences dialog window itself has no real UI yet (~20 controls, no real prefs-file read/write pathway to back it — same category of gap as C188/C189's own unwired dialogs); `GUI_Silent`/`GUI_KeepOpen` aren't wired since their real consumers (silent-mode extraction, batch-queue-empty self-relaunch) don't exist in this port's GUI yet; D010's remaining GUI-only preferences beyond what's covered here. Tests: `gui::prefs_dialog::tests` (15). UniExtract.au3:6303-6372,6372-6555 |
| C191 | First-start wizard — multi-page onboarding flow with dynamic Prev/Next/Finish button relabeling and per-page launch of the Preferences/Context-menu dialogs | interface | code | none found (directory check only) | REQUIRED | | **Partial, PR [#462](https://github.com/baileyrd/rusty_extract/pull/462).** `gui::first_start` ports the page-navigation state machine as a pure function of the *current* page (`prev_button_visible`, `next_button_mode`, `resolve_action_button`) — a reformulation of `GUI_FirstStart_Prev`/`_Next`/`_ShowPage`'s own delta-toggling side effects into one declarative function per widget, verified equivalent by tracing both the forward and backward traversal the wizard's fixed 3-page structure actually produces. Also ports `decide_missing_translation_outcome`, documenting (not wiring) the missing-`FIRSTSTART_PAGES`-translation branch's real "always exits either way, only conditionally restarts into an update first" shape. **Deliberately not wired to any real `SavePref("ID", "")`/exit call** — per this row's own note, that's a hard-to-reverse, unusually severe response to a missing string that needs explicit sign-off before real implementation, not just porting. **The wizard window itself stays unwired**: its own pages link out to `GUI_Prefs`/`GUI_ContextMenu` (C190/C192), neither a real window yet, and its text needs the translation-catalog infrastructure (D006) this port doesn't have. Tests: `gui::first_start::tests` (6). UniExtract.au3:7327-7427 |
| C192 | Plugin manager GUI — plugin list (hardcoded in-code table, not a manifest file), "download" (opens a webpage for manual fetch, no direct download), and "install" (extension-based dispatch: 7z/rar/zip extraction vs. direct file copy, with required-file validation) | interface+behavior | code | none found (directory check only) | REQUIRED | | **Partial, PR [#463](https://github.com/baileyrd/rusty_extract/pull/463).** `gui::plugin_manager` ports `resolve_install_mechanism` (unpack-vs-copy dispatch), `decide_select_close_action` (the overloaded Select/Finish button's close-vs-prompt guard), `resolve_plugin_selection_display` (Download/Select-Finish button label+enabled state per selection, preserving the `@Compiled`-gated installed-check quirk), `missing_required_files` (the wildcard-skip quirk), and `resolve_copy_plan` (single-file-renamed vs. multi-file-kept-named destinations, reusing `gui::file_input::parse_file_dialog_result`, C186, for the multi-select path). **Correction to this row's own earlier inventory note**: the previously-flagged `StringRight($sPath, 3) == ".7z"` "bug" doesn't exist — `".7z"` is 3 characters, not 4 as originally miscounted, so `StringRight(...,3)` extracts it correctly and `.7z` archives *do* route to the 7-Zip-extraction branch alongside `rar`/`zip`; verified precisely by recounting rather than trusting the earlier claim. Also covers D008's `/plugins` CLI verb (out of scope for the same reason as C007-C013: no CLI-flag parsing beyond positional arguments exists in this port's composition root). **Not yet done, stays `REQUIRED`**: the plugin manager window itself has no real UI — a list-plus-description window built around a hardcoded 12-entry table with real network downloads (`OpenURL`) and filesystem installs, none of which this port's GUI drives yet. Tests: `gui::plugin_manager::tests` (16). UniExtract.au3:7778-7944 |
| C193 | Generic prompt/confirm dialogs and checkbox/state helper primitives — a plain OK/Cancel-style prompt with a silent-mode auto-affirm shortcut, a 4-button Yes/No/Always/Never prompt with a persisted tri-state setting, and small checkbox/control-state read/write helpers used throughout the GUI | interface+behavior | code | none found (directory check only) | REQUIRED | | **Partial, PR [#464](https://github.com/baileyrd/rusty_extract/pull/464).** `gui::prompt` ports `Prompt`'s full dispatch (`is_affirmative_msgbox_response`/`decide_prompt_outcome` — silent-mode auto-affirm, the load-bearing behavior several already-DONE capabilities' `user_confirmed_*` stand-ins reference, e.g. `cleanup`'s C158 and `batch`'s C147) and `CustomPrompt`'s full dispatch (`CustomPromptSetting`/`decide_custom_prompt_short_circuit`/`resolve_custom_prompt_button` — the persisted Always/Never short-circuit, checked *before* silent mode, and the dialog's own button-to-setting-mutation mapping). `_IsChecked`/`_IsAnyChecked`/`_SetState` (UniExtract.au3:6049-6071) aren't ported as functions — they're a Win32 control-state bitmask/loop that's moot once a checkbox is a plain `bool` field bound directly by `egui::Checkbox`, the same class of supersession as C183/C185/C187/C189/C190's own toolkit notes. **Neither dialog is wired to a real window**, same treatment as C188-C192's own unwired dialogs. Tests: `gui::prompt::tests` (11). UniExtract.au3:5980-6075 |
| C194 | Error dialogs with feedback/scan integration — a generic error dialog with a "send feedback" escape hatch, and the unknown-file-type error dialog that runs a signature scan, shows results, and offers to launch Exeinfo PE via a clickable logo | interface+behavior | code | none found (directory check only) | REQUIRED | | **Partial, PR [#465](https://github.com/baileyrd/rusty_extract/pull/465).** `gui::error_dialogs` ports `should_show_error_dialog` (the shared silent-mode no-op gate both dialogs open with), `resolve_copy_target` (`_GUI_FileScan`'s copy-selection-or-all decision, unified into one function instead of reproducing `GUI_Error_UnknownExt`'s inline duplicate of the same logic — per this row's own manifest note), `needs_vertical_scrollbar` (the two dialogs' *different* line-count thresholds — 13 for the standalone `_GUI_FileScan` dialog, 7 for `GUI_Error_UnknownExt`'s embedded, shorter edit box — both real, neither hardcoded into the function itself), and `resolve_unknown_ext_layout` (the 290px-vs-190px dynamic sizing and scan-section visibility gate on whether a signature scan actually found anything). The unknown-ext dialog's logo-image-as-Exeinfo-PE-launch-button and `_GUI_FileScan`'s clipboard write are real I/O with no decision logic to port — left as real-wiring concerns. **Neither dialog is wired to a real window**, same treatment as C188-C193's own unwired dialogs. Tests: `gui::error_dialogs::tests` (8). UniExtract.au3:7611-7741 |
| C195 | Misc file/log utility actions — open-last-log, open-log-directory, delete-all-logs, log-menu-item size refresh, open-password-list-file (touch-if-missing), open-program-directory, open-config-file | behavior | code | none found (directory check only) | REQUIRED | | **Partial, PR [#466](https://github.com/baileyrd/rusty_extract/pull/466).** `gui::log_actions` ports `should_open_a_log`/`resolve_most_recent_log` (`GUI_OpenLastLog`'s early-return gate and its "most recent" selection — preserved exactly as the alphabetical-not-chronological assumption the source itself relies on, not silently upgraded to a real mtime sort), `should_update_log_menu_item`/`format_log_dir_size_mb` (`GUI_UpdateLogItem`'s main-window-exists gate and its `Round(...,2)` MB formatting), and `should_create_password_file` (`GUI_Password`'s touch-if-missing gate). `GUI_OpenLogDir`/`GUI_ProgDir`/`GUI_ConfigFile` are plain `ShellExecute` one-liners with no decision logic — not ported as functions. **Not yet done, stays `REQUIRED`**: none of this is wired to real I/O — `logdir` itself (`$settingsdir & "\log\"`) depends on settings-directory resolution this port's GUI doesn't have plumbed into its own state yet, the same category of gap as every other still-unresolved preference path (C183, C185, C188-C190). Tests: `gui::log_actions::tests` (6). UniExtract.au3:7944-7983,8064-8084 |
| C196 | Local usage statistics — per-status and per-archive-type counters written to a local (never transmitted) INI section after every extraction, and a pie-chart dialog displaying them once enough data exists | behavior+interface | code | none found (directory check only) | REQUIRED | | **Partial, PR [#467](https://github.com/baileyrd/rusty_extract/pull/467).** `gui::stats` ports `status_ini_key` (the `Status`-to-INI-key string mapping, both sides of the shared "Statistics" keyspace), `should_increment_arctype_counter` (the archive-type counter's success-only gate), `classify_stats_key` (the four-bucket categorization, resolving this row's own "preserve or fix" question by preserving), `should_show_stats` (confirmed: the ≥10 gate counts distinct keys, not total extraction volume — verified against `IniReadSection`'s own documented return shape), and `top_n_by_count` (the descending-sort-then-cap-at-9 filter). **New verified quirk this row's own note didn't mention**: the source's classification `Switch` has no case at all for `movefailed`/`nofreespace`/`missingpart`/`trayexit` — four real status keys that fall through to `Case Else` and get miscounted as archive types rather than landing in `Failed` or being excluded; preserved exactly, not "corrected." The GDI+ pie-chart rendering itself is out of scope entirely (a rendering dependency this port replaces, not a behavior to reproduce), so nothing here is wired to a real window. Tests: `gui::stats::tests` (8). UniExtract.au3:4125-4127,7983-8064 |
| C197 | About dialog and website menu actions — app name/version/build-timestamp/credits/logo, display of the per-install GUID, and three menu items opening fixed URLs | interface | code | none found (directory check only) | REQUIRED | | **Partial, PR [#468](https://github.com/baileyrd/rusty_extract/pull/468).** `gui::about` ports the one real decision — `resolve_about_logo_filename`, the high-contrast logo asset swap. Everything else in this row is either static string composition (version/timestamp/credits, and `$sOptGuid`'s direct display, C215) or fixed-URL `OpenURL` one-liners with no decision logic. `GUI_Close`'s multi-window active-detection trick (`$aGUIs`/`WinActive`) is moot under `egui`'s per-viewport model, where each window's own update closure already knows which window it's closing — not this row's own concern either way, since it isn't in this row's function list. **Not wired to a real window** — same treatment as every other dialog this phase has ported, doubly so since C215's per-install ID doesn't exist as GUI state yet either. Tests: `gui::about::tests` (1). UniExtract.au3:8084-8143 |
| C198 | Command-line help dialog — static, translated usage/flags text plus a documentation-website link | docs+interface | code | none found (directory check only) | REQUIRED | | **Verified: no pure decision logic exists to port.** Re-read `GUI_CommandLineHelp` in full (UniExtract.au3:7461-7496): every line either builds a window control from already-translated text (`t('HELP_*', ...)` string concatenation, blocked on the translation-catalog infrastructure, D006, still genuinely deferred) or is the one real behavior — the doc-link click, a plain `ShellExecute($sUrlCommandLineHelp)` with no branching at all. Unlike every other capability landed so far in this migration phase, there is no quirk, gate, or non-trivial mapping here to extract into a testable function; manufacturing one (e.g. a wrapper that just returns a fixed URL string) would be a hollow gesture with no real decision content, not a meaningful port. No source changes this pass — this row's own content is entirely blocked on D006 for its text and the general "no real window built yet" gap (C188-C197) for its container. Stays `REQUIRED` until both exist. UniExtract.au3:7461-7499 |
| C199 | Update-available/changelog prompt dialog — Yes/No dialog that synchronously fetches and displays remote changelog text as part of showing itself | interface+behavior+network | code | none found (directory check only) | REQUIRED | | **Partial, PR [#469](https://github.com/baileyrd/rusty_extract/pull/469).** `gui::update_prompt::resolve_changelog_text` ports the one real content decision: the translated `DOWNLOAD_FAILED` fallback on a fetch error. The Yes/No result itself reuses `CustomPrompt`'s (C193) already-established trivial boolean dialog-outcome shape, not re-derived. **Open design question, explicitly not resolved here**: whether to reproduce the source's blocking-fetch-during-dialog-construction shape verbatim, or restructure as a real async fetch-then-populate (more idiomatic for `egui`'s immediate-mode loop, which would otherwise freeze for the fetch's duration) — flagged for whoever wires the real dialog, per this row's own manifest note, rather than silently picked. **Not wired to a real window.** Tests: `gui::update_prompt::tests` (2). UniExtract.au3:7741-7778 |
| C200 | Missing-plugin and missing-FFmpeg first-run dialogs — prompts shown when a required helper executable can't be found, including FFmpeg's license-acceptance gate and its hardlink-then-copy-fallback install mechanism | interface+behavior | code | none found (directory check only) | REQUIRED | | The two dialogs inside `HasPlugin`/`HasFFMPEG` (UniExtract.au3:3735-3771 and ~3810-3883). Silent-mode terminates immediately with a specific status/error instead of showing UI. FFmpeg validation is a fragile size+substring heuristic (≥1MB and stdout contains "ffmpeg version") — preserve exactly if parity is the goal, flag as fragile rather than "fixing" unasked. The hardlink-over-copy install preference is a documented, deliberate architectural choice (persists after deleting the original, avoids symlink Windows-XP compatibility issues) worth preserving as an explicit comment in the port, not just replicated code. UniExtract.au3:3735-3883 |
| C201 | Explorer context-menu registration dialog and current-state detection — per-verb checkboxes, simple-vs-cascading mode selection, all-users gating, and probing both per-user and all-users registry roots to reflect current registration state | interface+behavior | code | none found (directory check only) | REQUIRED | | `GUI_ContextMenu`, `GUI_ContextMenu_ChangePic`, `GUI_ContextMenu_activate`. Real quirk: state-detection probes per-user (HKCU) after all-users (HKCR) for each verb, so if both scopes happen to have stale registrations simultaneously, the per-user check silently wins the displayed toggle state, potentially misrepresenting an all-users install as per-user. No self-elevation/UAC prompt exists — all-users checkboxes are just disabled (with a tooltip) unless already running elevated. UniExtract.au3:7038-7150,7216-7237 |
| C202 | Context-menu registry write/apply — writes per-verb shell-menu registry entries in either simple or Windows-7+ cascading (sub-menu) form, always via a full remove-then-rebuild rather than an incremental diff | behavior | code | none found (directory check only) | REQUIRED | | `GUI_ContextMenu_OK`. **Requires extending `automation::win32` first**: it currently only supports `REG_DWORD` writes under `HKCU`/`HKLM` (`reg_write_dword`); this capability needs `HKEY_CLASSES_ROOT` root support plus `REG_SZ`/`REG_EXPAND_SZ` string-value writes — scope that extension as part of this capability's own implementation PR, not a separate row. Real quirks: enabling with no individual verb checked self-corrects to "all verbs on" rather than leaving the feature enabled-but-empty; the icon registry value is only written on Windows 7+ (skipped on older Windows); cascading mode is gated on both the radio selection and a live Windows-7-or-newer check. UniExtract.au3:7150-7216 |
| C203 | Context-menu registry removal/teardown — deletes all verb entries (and the Win7+ cascading parent key) from both per-user and all-users roots, always run before any re-registration | behavior | code | none found (directory check only) | REQUIRED | | `GUI_ContextMenu_remove`. Confirms the registration model is destroy-then-recreate, not incremental — the Rust port's registry-diffing strategy should mirror this rather than attempt in-place diffs. Also tears down file associations (C204) if they were enabled. Same `automation::win32` HKCR-support dependency as C202. UniExtract.au3:7309-7327 |
| C204 | File-association registration — per-extension ProgID registry chain install/uninstall, driven by a user-supplied comma-separated extension list, with a modal "this is dangerous" confirmation before enabling | behavior+config | code | none found (directory check only) | REQUIRED | | `_ShellFile_Install`, `_ShellFile_Uninstall`, `GUI_ContextMenu_fileassoc`. Folds in D011's `addassocenabled`/`addassoc`/`addassocallusers` preferences. Real quirks: always tears down every currently-tracked extension before re-creating on enable, even when disabling; on disable, only `addassocenabled` is reset — the extension list and all-users scope preferences are left intact in storage, ready to reactivate with the same values; `_ShellFile_Install` writes what looks like two overlapping association styles (a proper `shell\open\command` subtree plus redundant top-level keys) — `_ShellFile_Uninstall` relies on recursive key deletion to clean up the whole subtree, so the port must verify its registry-delete primitive is actually recursive (not assumed) before relying on it, another dependency on `automation::win32`'s registry layer alongside C202/C203. UniExtract.au3:7237-7309 |
| C205 | CLI update-verb dispatch — `/update`, `/updatehelper`, `/updatehelpers`, `/afterupdate` argument parsing and routing into the updater | interface | code | none found (directory check only) | REQUIRED | | The dispatch block itself (UniExtract.au3:605-632), covering D007. Real quirk to preserve or flag: `/updatehelper` is matched case-insensitively (AutoIt's default `=`) while `/updatehelpers` is matched case-sensitively (`==`) — a real inconsistency between the two spellings, not obviously intentional. UniExtract.au3:601-632 |
| C206 | Update-index fetch and diff logic — fetches a remote plaintext file/size/hash index, recursively expands subdirectory entries, and compares local files against it to decide whether an update exists | behavior | code | none found (directory check only) | REQUIRED | | `_UpdateGetIndex`, `_UpdateGetSize`, `_UpdateFileCompare`, `CheckUpdateHelpers`. Wire format (undocumented, must capture precisely): LF-separated rows, comma-separated columns (`path,size,md5`). Real quirks: directories are compared by size only, never hash — a same-size-different-content directory is silently reported as up to date; MD5 is only computed when sizes already match (an optimization, not a correctness issue); `_UpdateGetSize`'s plugin-binary exclusion list is a hardcoded ~16-entry list that must be manually kept in sync with any new plugin binary added elsewhere, or size comparisons silently go stale; progress-percentage math caps its denominator at a minimum of 200, so it never visually reaches 100% with fewer than 200 total index entries — cosmetic, not a correctness bug, but worth preserving if the progress UI is ported faithfully. UniExtract.au3:5448-5477,5580-5637 |
| C207 | Main update orchestration — end-to-end update check: interval debounce, main-executable comparison and relaunch, helper-file update delegation, FFmpeg update delegation, elevation-aware relaunch/exit paths | behavior+config | code | none found (directory check only) | REQUIRED | | `CheckUpdate`. Folds in D003's nightly/beta channel switch and D012's `updateinterval`/`lastupdate` preferences. Real quirks: `$lastupdate` persistence is silently skipped entirely if preferences haven't been loaded yet at call time; declining the main-executable update prompt also suppresses the helper-files check for that same run (not independent); a main-update-accepted path exits via a raw `Exit`, bypassing the app's normal `terminate()` cleanup/logging — an inconsistent exit path worth preserving or flagging; a successful "no update, running elevated" path ends by de-elevating and exiting, so the CLI dispatcher (C205) never sees this call return normally. The GUI-decision seams (`GUI_UpdatePrompt` from C199, `Prompt` from C193) gate real control flow here and need a clean interface boundary at exactly those two call points. UniExtract.au3:5374-5448 |
| C208 | Helper-file download and install — downloads and applies updated helper/program files with per-file and overall progress reporting | behavior | code | none found (directory check only) | REQUIRED | | `_UpdateHelpers`. Real quirks: duplicates `CheckUpdateHelpers`' recursive directory-expansion logic independently (a DRY gap in the source, not necessarily worth preserving as duplicated code in the port, but the *behavior* of both must still match); always deletes an existing local file before overwriting, an explicit documented workaround for an unexplained Windows overwrite-failure condition; a per-file download error uses a double-level loop continue to skip that file and keep going rather than aborting the whole update; sends a "success" telemetry ping unconditionally at the end even if individual files failed; overall progress bar fakes a small increment when real progress hasn't advanced, purely for visual smoothness. The download loop's per-file/network logic is decouplable from the inline progress-dialog updates via a callback, independent of whether egui or another approach renders the progress UI. UniExtract.au3:5480-5553 |
| C209 | FFmpeg update check — compares locally-installed FFmpeg's reported version against a remote version string and offers to download an update | behavior | code | none found (directory check only) | REQUIRED | | `_UpdateFFmpeg`, `GetFFmpeg`. Real bug to flag (default: preserve, per this migration's convention, unless told otherwise): version comparison is a plain lexicographic string `>` comparison, not numeric/semver — decide fix-vs-preserve explicitly at implementation time rather than silently changing it. Remote version URL varies by OS (WinXP and 32-bit get distinct suffixes) — an assumption that only these two special variants exist. A failed local version-string extraction is deliberately treated as "force update" (documented defensive behavior, not a bug). UniExtract.au3:5553-5580,5772-5782 |
| C210 | Post-update migration — the `/afterupdate` handler's one-time file/directory/ini-key cleanup steps applied after an update completes | behavior | code | none found (directory check only) | REQUIRED | | `_AfterUpdate`. This is real, historically-accumulated upgrade-path behavior (DLL relocations, ~30 hardcoded deprecated-binary deletions, directory removals, obsolete ini-key cleanup) that must be ported **verbatim**, not "cleaned up" — dropping any single line would leave cruft behind for a real user upgrading from an old installed version. No branching logic beyond one `FileExists` sentinel check; the value here is completeness, not cleverness. UniExtract.au3:5673-5769 |
| C211 | Update-failure/restart/relaunch mechanics — helper-process termination, self-relaunch (elevated and de-elevated), update-check failure/retry dialogs, and the custom progress-dialog primitives the updater uses | behavior+interface | code | none found (directory check only) | REQUIRED | | `KillHelper`, `Restart`, `RestartWithoutAdminRights`, `RepairProgramFiles`, `_UpdateCheckFailed`, `_ProgressOn`, `_ProgressSet`, `_ProgressOff`, `_DeleteFromArchDir`. `_ProgressOn`'s dialog handle is a bare global (no stacking/nesting support — only one progress dialog app-wide at a time), a real architectural constraint worth a deliberate decision in the port's own progress-reporting design rather than silently assuming it doesn't matter. `RestartWithoutAdminRights` concatenates its parameter string into a shell command with no quoting/escaping — a real robustness gap to flag. UniExtract.au3:5316-5347,5640-5670,5782-5791 |
| C212 | Feedback dialog and submission — system-info/log/message capture dialog, remote-version staleness check, and the hand-rolled multipart HTTP POST submitting it all to a fixed endpoint | interface+behavior+network | code | none found (directory check only) | REQUIRED | | `GUI_Feedback`, `GUI_Feedback_Outdated`, `GUI_Feedback_Send`, `GUI_Feedback_Error`. **Heaviest PII surface in this phase** — scope carefully: transmits the sample file's name/size/MD5, full captured extraction log (which can itself contain a hex dump + extended file metadata logged earlier in the same flow), free-text user message, OS/locale/version info, and the per-install GUID (C215), all gated behind an explicit privacy-policy checkbox already present in the source. Real quirks: success detection is a bare string-equality check against literal `"1"` in the HTTP response body, no status-code check at all; no request timeout, so a hung connection blocks indefinitely with the main window hidden; gzip-vs-plain body encoding is chosen by language or a >1MB size threshold; attaching a sample file triggers a side-effecting hex-dump-to-log step unconditionally, independent of whether that file ends up in the actual submission. UniExtract.au3:6756-6935 |
| C213 | Feedback-prompt gate — decides whether to offer the feedback dialog after specific extraction outcomes, with a persisted tri-state (never/ask/always) preference | interface+behavior+config | code | none found (directory check only) | REQUIRED | | `GUI_Feedback_Prompt`. Folds in D012's `feedbackprompt` preference. Only triggered for a specific, easy-to-lose set of exit statuses (excluding `.dll` files) — that trigger condition lives in the caller, not this function itself, and must be captured alongside it, not just this function's own body. "Always send" still requires the privacy checkbox to be checked on the actual feedback form — remembering "yes" doesn't bypass consent. UniExtract.au3:4226,6938-6981 |
| C214 | Usage-stats telemetry beacon — lightweight, fire-and-forget HTTP GET ping sent from many call sites across the update/uninstall/feedback/extraction-exit flows | behavior+network | code | none found (directory check only) | REQUIRED | | `SendStats`. Folds in D012's `sendstats` preference (including the Prefs-dialog enable/disable-ping ordering quirk: the "disable" ping is sent using the *old*, still-true value, immediately before the flag actually flips off — an ordering dependency that must be preserved exactly or the opt-out event silently never fires). No error handling at all (network failure is silently swallowed); the second URL parameter's meaning is overloaded per call site (sometimes a result code, sometimes an archive-type string) — capture the real meaning per call site rather than assuming one consistent semantic; logs the full outgoing URL (including the GUID) to the local debug log. UniExtract.au3:5366-5374 |
| C215 | Per-install GUID generation — a persistent pseudonymous identifier created on first run and attached to both telemetry channels (C212, C214) and shown in the About dialog (C197) | config+behavior | code | none found (directory check only) | REQUIRED | | The GUID generation/load/save mechanism (UniExtract.au3:157,335-341,776). Real quirk to flag: the fallback path (used only if the primary OS GUID API returns empty) derives "randomness" from AutoIt's non-cryptographic `Random()` feeding an AES encrypt call that adds no real entropy beyond the two `Random()` outputs — a meaningfully smaller and more guessable ID space than a true GUID; decide whether to preserve this exact fallback shape or intentionally strengthen it, with sign-off either way. The ID is permanently prefixed with a hardcoded version-family tag and never updates after upgrades — preserve this prefix format if the same telemetry endpoint/backend is ever kept. UniExtract.au3:157,335-341,776 |
| C216 | Uninstall orchestration and CLI dispatch — the `/uninstall`/`/removeuserdata` CLI path and the actual delete sequence (context-menu/file-association cleanup, logs, optional user-data wipe) | interface+behavior | code | none found (directory check only) | REQUIRED | | `Uninstall`, plus the `/uninstall`/`/removeuserdata` CLI dispatch (covering D009). **Real bug found, decide preserve-vs-fix at implementation**: the CLI path passes `_ArraySearch`'s raw return value (an index, or `-1` if not found — never `0`) directly as the "remove user data" boolean; since AutoIt treats any nonzero number including `-1` as truthy, a silent `/uninstall` invocation effectively **always** wipes user data regardless of whether `/removeuserdata` was actually supplied, diverging from the GUI path (C217), which correctly passes a real checkbox-derived boolean. No confirmation, no per-step error handling — a hard recursive delete of the settings directory with no partial-failure reporting. Unconditionally, synchronously calls into C203/C204's registry teardown before touching files. UniExtract.au3:601-628,5791-5801 |
| C217 | Uninstall confirmation dialog — the interactive "remove logs" / "remove user data" checkbox dialog gating the actual uninstall sequence | interface | code | none found (directory check only) | REQUIRED | | `GUI_Uninstall`. Real quirk to flag as a UX decision for the port, not silently replicate or silently fix: the dialog's system Close (X) button is explicitly disabled and there is no Cancel button — the only way out is clicking "Uninstall" itself. UniExtract.au3:7427-7461 |
